# claude-app-server Phase 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Agent SDK の `tool_use` / `tool_result` を Codex 風の `commandExecution` / `fileChange` / `mcpToolCall` item にマッピングし、`item/started` → `item/completed` のライフサイクル通知を Symphony に届ける。

**Architecture:** `SdkMessage.parse` をブロック単位分解 (戻り値配列化) に変えて 1 assistant 内の text/tool_use 混在を正しく取り扱う。`EventMapper.MapperState` に pending tool map を追加し、tool ライフサイクルを純粋関数で表現。tool 種別ごとの item shape 構築は新規 `tool-item.ts` (純粋関数) に切り出す。`session.ts` 側は `SdkMessage[]` ループと `EventMapper.closeAll(reason)` 呼び出しに変更。

**Tech Stack:** TypeScript ESM (NodeNext) + vitest + valibot + Claude Agent SDK。`pnpm test` / `pnpm build` / `pnpm lint` / `pnpm format:check` で検証。

**Spec:** [`docs/superpowers/specs/2026-05-12-cas-phase3-design.md`](../specs/2026-05-12-cas-phase3-design.md)

---

## ファイル構成

新規:
- `apps/claude-app-server/src/claude/tool-item.ts` — `tool_use` → Codex item 構築 (純粋関数)
- `apps/claude-app-server/test/claude/tool-item.test.ts`
- `apps/claude-app-server/README.md`

変更:
- `apps/claude-app-server/src/claude/sdk-message.ts` — `parse(raw): SdkMessage[]` に変更、assistant をブロック分解
- `apps/claude-app-server/src/claude/event-mapper.ts` — `pendingTools` 追加、tool 系遷移、`closeAll(reason)` 追加、`ResultSuccess`/`ResultError` を no-op 化
- `apps/claude-app-server/src/claude/session.ts` — `SdkMessage[]` ループ対応、`closeAgentMessageIfAny` を `closeAll` に置換
- `apps/claude-app-server/test/claude/sdk-message.test.ts` — 配列化に追従、混在/複数 yield ケース追加
- `apps/claude-app-server/test/claude/event-mapper.test.ts` — tool ライフサイクル、`closeAll`、並列 tool_use
- `apps/claude-app-server/examples/manual_test.jsonl` — tool シナリオに拡張
- `TODO.md` — Phase 3 チェックボックス更新

ブランチ: `feat/cas-phase3` (既に切ってある、spec が `538698f` で commit 済み)。

---

## Task 1: `SdkMessage.parse` をブロック単位分解 + 戻り値配列化

**Files:**
- Modify: `apps/claude-app-server/src/claude/sdk-message.ts`
- Modify: `apps/claude-app-server/src/claude/session.ts`
- Modify: `apps/claude-app-server/test/claude/sdk-message.test.ts`

- [ ] **Step 1: 既存テストを配列化に追従**

`test/claude/sdk-message.test.ts` の全 `expect(parsed.kind).toBe(...)` 系を `expect(parsed).toHaveLength(1)` + `expect(parsed[0]?.kind).toBe(...)` パターンに書き換える (テスト本体の意図は保つ)。`if (parsed.kind === ...)` ナローイングは `const msg = parsed[0]; if (msg?.kind === ...)` に直す。

例:

```typescript
it("parses system.init into SystemInit", () => {
  const parsed = SdkMessage.parse({ /* ... */ });
  expect(parsed).toHaveLength(1);
  const msg = parsed[0];
  expect(msg?.kind).toBe("SystemInit");
  if (msg?.kind === "SystemInit") {
    expect(msg.sessionId).toBe("sess-abc");
  }
});
```

- [ ] **Step 2: 新規 failing tests を追加**

`test/claude/sdk-message.test.ts` の末尾に以下を追加:

```typescript
it("yields text then tool_use in order when both appear in one assistant message", () => {
  const parsed = SdkMessage.parse({
    type: "assistant",
    message: {
      content: [
        { type: "text", text: "let me run" },
        { type: "tool_use", id: "tu_1", name: "Bash", input: { command: "ls" } },
      ],
    },
    session_id: "s",
    parent_tool_use_id: null,
    uuid: "u",
  });
  expect(parsed.map((m) => m.kind)).toEqual(["AssistantText", "AssistantToolUse"]);
});

it("yields multiple tool_use blocks in order", () => {
  const parsed = SdkMessage.parse({
    type: "assistant",
    message: {
      content: [
        { type: "tool_use", id: "tu_1", name: "Bash", input: { command: "ls" } },
        { type: "tool_use", id: "tu_2", name: "Read", input: { file_path: "/a" } },
      ],
    },
    session_id: "s",
    parent_tool_use_id: null,
    uuid: "u",
  });
  expect(parsed).toHaveLength(2);
  expect(parsed[0]?.kind).toBe("AssistantToolUse");
  expect(parsed[1]?.kind).toBe("AssistantToolUse");
});

it("yields multiple tool_result blocks from a user message in order", () => {
  const parsed = SdkMessage.parse({
    type: "user",
    message: {
      content: [
        { type: "tool_result", tool_use_id: "tu_1", content: "ok1" },
        { type: "tool_result", tool_use_id: "tu_2", content: "ok2" },
      ],
    },
    session_id: "s",
    parent_tool_use_id: null,
    uuid: "u",
  });
  expect(parsed).toHaveLength(2);
  expect(parsed[0]?.kind).toBe("UserToolResult");
  expect(parsed[1]?.kind).toBe("UserToolResult");
});

it("returns [Unknown] for unrecognized payloads", () => {
  const parsed = SdkMessage.parse({ type: "wat" });
  expect(parsed).toHaveLength(1);
  expect(parsed[0]?.kind).toBe("Unknown");
});

it("skips unknown content blocks in assistant message but keeps known ones", () => {
  const parsed = SdkMessage.parse({
    type: "assistant",
    message: {
      content: [
        { type: "thinking", text: "internal" },
        { type: "text", text: "visible" },
      ],
    },
    session_id: "s",
    parent_tool_use_id: null,
    uuid: "u",
  });
  expect(parsed).toHaveLength(1);
  expect(parsed[0]?.kind).toBe("AssistantText");
});
```

- [ ] **Step 3: テストを実行して fail を確認**

Run: `cd apps/claude-app-server && pnpm test -- sdk-message`
Expected: 既存テスト + 新規テストの両方で TypeScript エラーまたは assertion failure。

- [ ] **Step 4: `SdkMessage.parse` を再設計**

`src/claude/sdk-message.ts` の `parse` 戻り値を `SdkMessage[]` に変更。`parseAssistant` をブロック単位走査に書き換え、`parseUser` も同様に複数 `tool_result` を yield。

実装の骨格:

```typescript
const parseAssistantBlocks = (raw: unknown): SdkMessage[] => {
  const r = v.safeParse(AssistantSchema, raw);
  if (!r.success) return [];
  const out: SdkMessage[] = [];
  for (const block of r.output.message.content) {
    const tb = v.safeParse(TextBlockSchema, block);
    if (tb.success) {
      out.push({ kind: "AssistantText", text: tb.output.text, sessionId: r.output.session_id });
      continue;
    }
    const tu = v.safeParse(ToolUseBlockSchema, block);
    if (tu.success) {
      out.push({
        kind: "AssistantToolUse",
        toolUseId: tu.output.id,
        name: tu.output.name,
        input: tu.output.input,
        sessionId: r.output.session_id,
      });
      continue;
    }
    // 未知ブロックは黙殺
  }
  return out;
};

const parseUserBlocks = (raw: unknown): SdkMessage[] => {
  const r = v.safeParse(UserSchema, raw);
  if (!r.success) return [];
  const out: SdkMessage[] = [];
  for (const block of r.output.message.content) {
    const tr = v.safeParse(ToolResultBlockSchema, block);
    if (tr.success) {
      out.push({
        kind: "UserToolResult",
        toolUseId: tr.output.tool_use_id,
        content: tr.output.content,
        sessionId: r.output.session_id,
      });
    }
  }
  return out;
};

export const SdkMessage = {
  parse: (raw: unknown): SdkMessage[] => {
    const sysInit = v.safeParse(SystemInitSchema, raw);
    if (sysInit.success)
      return [{ kind: "SystemInit", sessionId: sysInit.output.session_id }];

    const resOk = v.safeParse(ResultSuccessSchema, raw);
    if (resOk.success)
      return [{
        kind: "ResultSuccess",
        sessionId: resOk.output.session_id,
        resultText: resOk.output.result,
      }];

    const resErr = v.safeParse(ResultErrorSchema, raw);
    if (resErr.success)
      return [{
        kind: "ResultError",
        sessionId: resErr.output.session_id,
        subtype: resErr.output.subtype,
        errors: resErr.output.errors ?? [],
      }];

    const asst = parseAssistantBlocks(raw);
    if (asst.length > 0) return asst;

    const user = parseUserBlocks(raw);
    if (user.length > 0) return user;

    return [{ kind: "Unknown", raw }];
  },
} as const;
```

`UserToolResultBlock` で `content: v.unknown()` のままなので tool_result 内部の content 構造変更は影響しない。

- [ ] **Step 5: `session.ts` のループ追加 (ビルドを通す)**

`src/claude/session.ts` のメインループを以下に変更:

```typescript
for await (const raw of deps.query({ prompt: promptFrom(args.input), options: sdkOpts })) {
  for (const parsed of SdkMessage.parse(raw)) {
    if (parsed.kind === "SystemInit") {
      const sid = SessionId.parse(parsed.sessionId);
      if (sid.type === "Success") threads.setSessionId(thread.id, sid.value);
    }
    const r = EventMapper.map(parsed, mapperState, mapCtx);
    mapperState = r.state;
    for (const n of r.notifications) sendNotification(n);

    if (parsed.kind === "ResultError") {
      status = "failed";
      error = { message: parsed.errors.join("; ") || `result error: ${parsed.subtype}` };
    }
  }
}
```

`closeAgentMessageIfAny` 周りは Task 9-10 で削除するのでこの時点では残す。

- [ ] **Step 6: テスト + ビルド + lint + format 確認**

Run:
```bash
cd apps/claude-app-server && pnpm test && pnpm build && pnpm lint && pnpm format:check
```
Expected: 全 PASS。

- [ ] **Step 7: Commit**

```bash
git add apps/claude-app-server/src/claude/sdk-message.ts \
        apps/claude-app-server/src/claude/session.ts \
        apps/claude-app-server/test/claude/sdk-message.test.ts
git commit -m "$(cat <<'EOF'
refactor(claude-app-server): split assistant content into per-block SdkMessage[]

Phase 3 step 1. SdkMessage.parse now returns SdkMessage[] so a single
assistant message with interleaved text and tool_use blocks fans out
into ordered DU variants. Required for tool-call mapping in upcoming
tasks. session.ts loops over the array; behavior unchanged otherwise.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: `tool-item.ts` の dispatch + `normalizeToolResultContent`

**Files:**
- Create: `apps/claude-app-server/src/claude/tool-item.ts`
- Create: `apps/claude-app-server/test/claude/tool-item.test.ts`

- [ ] **Step 1: failing tests を書く**

`test/claude/tool-item.test.ts` を新規作成:

```typescript
import { describe, expect, it } from "vitest";
import { ToolItem } from "../../src/claude/tool-item.js";

describe("ToolItem.dispatch", () => {
  it("returns commandExecution for Bash", () => {
    expect(ToolItem.dispatch("Bash")).toBe("commandExecution");
  });
  it("returns fileChange for Edit / Write / MultiEdit", () => {
    expect(ToolItem.dispatch("Edit")).toBe("fileChange");
    expect(ToolItem.dispatch("Write")).toBe("fileChange");
    expect(ToolItem.dispatch("MultiEdit")).toBe("fileChange");
  });
  it("returns mcpToolCall for everything else", () => {
    expect(ToolItem.dispatch("Read")).toBe("mcpToolCall");
    expect(ToolItem.dispatch("Glob")).toBe("mcpToolCall");
    expect(ToolItem.dispatch("Grep")).toBe("mcpToolCall");
    expect(ToolItem.dispatch("WebFetch")).toBe("mcpToolCall");
    expect(ToolItem.dispatch("SomeFutureTool")).toBe("mcpToolCall");
  });
});

describe("ToolItem.normalizeToolResultContent", () => {
  it("returns the string as-is when content is a string", () => {
    expect(ToolItem.normalizeToolResultContent("hello")).toBe("hello");
  });
  it("concatenates text blocks when content is an array of blocks", () => {
    const out = ToolItem.normalizeToolResultContent([
      { type: "text", text: "foo " },
      { type: "text", text: "bar" },
    ]);
    expect(out).toBe("foo bar");
  });
  it("falls back to JSON.stringify for non-text blocks in array", () => {
    const out = ToolItem.normalizeToolResultContent([
      { type: "image", source: { url: "x" } },
    ]);
    expect(out).toContain("image");
  });
  it("falls back to JSON.stringify for unknown content shapes", () => {
    expect(ToolItem.normalizeToolResultContent({ weird: true })).toBe('{"weird":true}');
  });
});
```

- [ ] **Step 2: テストを実行して fail を確認**

Run: `cd apps/claude-app-server && pnpm test -- tool-item`
Expected: `Cannot find module '../../src/claude/tool-item.js'`.

- [ ] **Step 3: `tool-item.ts` を作成 (dispatch + normalizeToolResultContent のみ)**

`src/claude/tool-item.ts` を新規作成:

```typescript
export type ToolItemType = "commandExecution" | "fileChange" | "mcpToolCall";

const dispatch = (name: string): ToolItemType => {
  switch (name) {
    case "Bash":
      return "commandExecution";
    case "Edit":
    case "Write":
    case "MultiEdit":
      return "fileChange";
    default:
      return "mcpToolCall";
  }
};

const normalizeToolResultContent = (content: unknown): string => {
  if (typeof content === "string") return content;
  if (Array.isArray(content)) {
    const parts: string[] = [];
    for (const block of content) {
      if (
        block !== null &&
        typeof block === "object" &&
        "type" in block &&
        (block as { type: unknown }).type === "text" &&
        "text" in block &&
        typeof (block as { text: unknown }).text === "string"
      ) {
        parts.push((block as { text: string }).text);
      } else {
        parts.push(JSON.stringify(block));
      }
    }
    return parts.join("");
  }
  return JSON.stringify(content);
};

export const ToolItem = {
  dispatch,
  normalizeToolResultContent,
} as const;
```

- [ ] **Step 4: テスト + lint + format 確認**

Run: `cd apps/claude-app-server && pnpm test -- tool-item && pnpm lint && pnpm format:check`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add apps/claude-app-server/src/claude/tool-item.ts \
        apps/claude-app-server/test/claude/tool-item.test.ts
git commit -m "$(cat <<'EOF'
feat(claude-app-server): add tool-item dispatch and content normalization

Phase 3 step 2. New module tool-item.ts (pure functions) routes Claude
tool names to Codex item types and normalizes tool_result content into
a single string (handling string / text-block array / fallback shapes).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `tool-item.ts` の Bash started/completed builder

**Files:**
- Modify: `apps/claude-app-server/src/claude/tool-item.ts`
- Modify: `apps/claude-app-server/test/claude/tool-item.test.ts`

- [ ] **Step 1: failing tests を追加**

`test/claude/tool-item.test.ts` の末尾に追加:

```typescript
import { ItemId } from "../../src/util/ids.js";

describe("ToolItem.buildStarted (Bash)", () => {
  it("builds a commandExecution started item from Bash tool_use", () => {
    const itemId = ItemId.fresh();
    const item = ToolItem.buildStarted({
      itemId,
      name: "Bash",
      input: { command: "ls -la", description: "list files" },
      cwd: "/tmp/ws",
    });
    expect(item).toEqual({
      id: itemId,
      type: "commandExecution",
      command: ["bash", "-c", "ls -la"],
      cwd: "/tmp/ws",
      status: "inProgress",
      commandActions: [],
      aggregatedOutput: "",
      exitCode: null,
      durationMs: null,
    });
  });
  it("falls back to empty string when Bash command is missing", () => {
    const itemId = ItemId.fresh();
    const item = ToolItem.buildStarted({
      itemId,
      name: "Bash",
      input: {},
      cwd: "/tmp",
    });
    expect((item as { command: string[] }).command).toEqual(["bash", "-c", ""]);
  });
});

describe("ToolItem.buildCompleted (Bash)", () => {
  it("populates aggregatedOutput and status: completed on success", () => {
    const itemId = ItemId.fresh();
    const started = ToolItem.buildStarted({
      itemId,
      name: "Bash",
      input: { command: "echo hi" },
      cwd: "/tmp",
    });
    const completed = ToolItem.buildCompleted({
      started,
      itemType: "commandExecution",
      content: "hi\n",
      isError: false,
    });
    expect(completed).toMatchObject({
      id: itemId,
      type: "commandExecution",
      status: "completed",
      aggregatedOutput: "hi\n",
      exitCode: null,
    });
  });
  it("sets status: failed when is_error is true", () => {
    const itemId = ItemId.fresh();
    const started = ToolItem.buildStarted({
      itemId,
      name: "Bash",
      input: { command: "false" },
      cwd: "/tmp",
    });
    const completed = ToolItem.buildCompleted({
      started,
      itemType: "commandExecution",
      content: "error: x",
      isError: true,
    });
    expect(completed).toMatchObject({
      status: "failed",
      aggregatedOutput: "error: x",
    });
  });
});
```

- [ ] **Step 2: テストを実行して fail を確認**

Run: `cd apps/claude-app-server && pnpm test -- tool-item`
Expected: `ToolItem.buildStarted is not a function`.

- [ ] **Step 3: `buildStarted` + `buildCompleted` を実装 (Bash 経路のみ)**

`src/claude/tool-item.ts` に追加:

```typescript
import * as v from "valibot";
import type { ItemId } from "../util/ids.js";

const BashInputSchema = v.object({
  command: v.optional(v.string()),
});

export type StartedItem = Readonly<Record<string, unknown>> & Readonly<{ id: ItemId; type: ToolItemType; status: "inProgress" }>;

export type CompletedStatus = "completed" | "failed";

const buildBashStarted = (args: { itemId: ItemId; input: unknown; cwd: string }): StartedItem => {
  const parsed = v.safeParse(BashInputSchema, args.input);
  const command = parsed.success ? parsed.output.command ?? "" : "";
  return {
    id: args.itemId,
    type: "commandExecution",
    command: ["bash", "-c", command],
    cwd: args.cwd,
    status: "inProgress",
    commandActions: [],
    aggregatedOutput: "",
    exitCode: null,
    durationMs: null,
  };
};

export type BuildStartedArgs = Readonly<{
  itemId: ItemId;
  name: string;
  input: unknown;
  cwd: string;
}>;

const buildStarted = (args: BuildStartedArgs): StartedItem => {
  const itemType = dispatch(args.name);
  switch (itemType) {
    case "commandExecution":
      return buildBashStarted({ itemId: args.itemId, input: args.input, cwd: args.cwd });
    case "fileChange":
      // Task 4 で実装
      throw new Error("fileChange not implemented in this task");
    case "mcpToolCall":
      // Task 5 で実装
      throw new Error("mcpToolCall not implemented in this task");
  }
};

const buildBashCompleted = (started: StartedItem, content: string, isError: boolean): StartedItem & { status: CompletedStatus } => ({
  ...started,
  status: isError ? "failed" : "completed",
  aggregatedOutput: content,
});

export type BuildCompletedArgs = Readonly<{
  started: StartedItem;
  itemType: ToolItemType;
  content: string;
  isError: boolean;
}>;

const buildCompleted = (args: BuildCompletedArgs): StartedItem & { status: CompletedStatus } => {
  switch (args.itemType) {
    case "commandExecution":
      return buildBashCompleted(args.started, args.content, args.isError);
    case "fileChange":
      throw new Error("fileChange not implemented in this task");
    case "mcpToolCall":
      throw new Error("mcpToolCall not implemented in this task");
  }
};

export const ToolItem = {
  dispatch,
  normalizeToolResultContent,
  buildStarted,
  buildCompleted,
} as const;
```

(Task 4-5 で他経路が埋まる前提で、未実装は throw。本タスクのテストは Bash 経路だけなので OK。)

- [ ] **Step 4: テスト + lint + format 確認**

Run: `cd apps/claude-app-server && pnpm test -- tool-item && pnpm lint && pnpm format:check`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add apps/claude-app-server/src/claude/tool-item.ts \
        apps/claude-app-server/test/claude/tool-item.test.ts
git commit -m "$(cat <<'EOF'
feat(claude-app-server): map Bash tool_use to commandExecution item

Phase 3 step 3. Adds buildStarted/buildCompleted for Bash. exitCode and
durationMs stay null per spec (Claude SDK does not surface them); the
fileChange and mcpToolCall branches throw until subsequent tasks fill
them in.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `tool-item.ts` の Edit / Write / MultiEdit → fileChange

**Files:**
- Modify: `apps/claude-app-server/src/claude/tool-item.ts`
- Modify: `apps/claude-app-server/test/claude/tool-item.test.ts`

- [ ] **Step 1: failing tests を追加**

```typescript
describe("ToolItem.buildStarted (fileChange)", () => {
  it("maps Edit to fileChange with kind:edit and simple diff", () => {
    const itemId = ItemId.fresh();
    const item = ToolItem.buildStarted({
      itemId,
      name: "Edit",
      input: { file_path: "/a.ts", old_string: "old", new_string: "new" },
      cwd: "/tmp",
    });
    expect(item).toMatchObject({
      id: itemId,
      type: "fileChange",
      status: "inProgress",
      changes: [{ path: "/a.ts", kind: "edit", diff: "< old\n> new" }],
    });
  });

  it("maps Write to fileChange with kind:create", () => {
    const itemId = ItemId.fresh();
    const item = ToolItem.buildStarted({
      itemId,
      name: "Write",
      input: { file_path: "/b.ts", content: "hello\nworld" },
      cwd: "/tmp",
    });
    expect(item).toMatchObject({
      type: "fileChange",
      changes: [{ path: "/b.ts", kind: "create", diff: "> hello\nworld" }],
    });
  });

  it("maps MultiEdit to fileChange with diff joined by ---", () => {
    const itemId = ItemId.fresh();
    const item = ToolItem.buildStarted({
      itemId,
      name: "MultiEdit",
      input: {
        file_path: "/c.ts",
        edits: [
          { old_string: "a1", new_string: "b1" },
          { old_string: "a2", new_string: "b2" },
        ],
      },
      cwd: "/tmp",
    });
    expect(item).toMatchObject({
      type: "fileChange",
      changes: [{ path: "/c.ts", kind: "edit", diff: "< a1\n> b1\n---\n< a2\n> b2" }],
    });
  });
});

describe("ToolItem.buildCompleted (fileChange)", () => {
  it("preserves changes from started and sets status:completed", () => {
    const itemId = ItemId.fresh();
    const started = ToolItem.buildStarted({
      itemId,
      name: "Edit",
      input: { file_path: "/x", old_string: "o", new_string: "n" },
      cwd: "/tmp",
    });
    const completed = ToolItem.buildCompleted({
      started,
      itemType: "fileChange",
      content: "File updated",
      isError: false,
    });
    expect(completed).toMatchObject({
      type: "fileChange",
      status: "completed",
      changes: started.changes,
    });
  });

  it("sets status:failed on is_error", () => {
    const itemId = ItemId.fresh();
    const started = ToolItem.buildStarted({
      itemId,
      name: "Write",
      input: { file_path: "/x", content: "x" },
      cwd: "/tmp",
    });
    const completed = ToolItem.buildCompleted({
      started,
      itemType: "fileChange",
      content: "permission denied",
      isError: true,
    });
    expect((completed as { status: string }).status).toBe("failed");
  });
});
```

- [ ] **Step 2: fail を確認**

Run: `cd apps/claude-app-server && pnpm test -- tool-item`
Expected: `Error: fileChange not implemented in this task`。

- [ ] **Step 3: fileChange 経路を実装**

`src/claude/tool-item.ts` に追加 (既存の `buildStarted` の `case "fileChange"` を埋める):

```typescript
const EditInputSchema = v.object({
  file_path: v.string(),
  old_string: v.string(),
  new_string: v.string(),
});

const WriteInputSchema = v.object({
  file_path: v.string(),
  content: v.string(),
});

const MultiEditInputSchema = v.object({
  file_path: v.string(),
  edits: v.array(v.object({ old_string: v.string(), new_string: v.string() })),
});

const buildEditDiff = (oldStr: string, newStr: string): string => `< ${oldStr}\n> ${newStr}`;

const buildFileChangeStarted = (args: { itemId: ItemId; name: string; input: unknown }): StartedItem => {
  if (args.name === "Edit") {
    const r = v.safeParse(EditInputSchema, args.input);
    if (!r.success) {
      return {
        id: args.itemId,
        type: "fileChange",
        status: "inProgress",
        changes: [{ path: "", kind: "edit", diff: "" }],
      };
    }
    return {
      id: args.itemId,
      type: "fileChange",
      status: "inProgress",
      changes: [{ path: r.output.file_path, kind: "edit", diff: buildEditDiff(r.output.old_string, r.output.new_string) }],
    };
  }
  if (args.name === "Write") {
    const r = v.safeParse(WriteInputSchema, args.input);
    if (!r.success) {
      return {
        id: args.itemId,
        type: "fileChange",
        status: "inProgress",
        changes: [{ path: "", kind: "create", diff: "" }],
      };
    }
    return {
      id: args.itemId,
      type: "fileChange",
      status: "inProgress",
      changes: [{ path: r.output.file_path, kind: "create", diff: `> ${r.output.content}` }],
    };
  }
  // MultiEdit
  const r = v.safeParse(MultiEditInputSchema, args.input);
  if (!r.success) {
    return {
      id: args.itemId,
      type: "fileChange",
      status: "inProgress",
      changes: [{ path: "", kind: "edit", diff: "" }],
    };
  }
  const diff = r.output.edits.map((e) => buildEditDiff(e.old_string, e.new_string)).join("\n---\n");
  return {
    id: args.itemId,
    type: "fileChange",
    status: "inProgress",
    changes: [{ path: r.output.file_path, kind: "edit", diff }],
  };
};
```

そして既存の `buildStarted` の `case "fileChange"` を以下に置き換える:

```typescript
case "fileChange":
  return buildFileChangeStarted({ itemId: args.itemId, name: args.name, input: args.input });
```

`buildCompleted` の `case "fileChange"`:

```typescript
case "fileChange":
  return { ...args.started, status: args.isError ? "failed" : "completed" };
```

- [ ] **Step 4: テスト + lint + format**

Run: `cd apps/claude-app-server && pnpm test -- tool-item && pnpm lint && pnpm format:check`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add apps/claude-app-server/src/claude/tool-item.ts \
        apps/claude-app-server/test/claude/tool-item.test.ts
git commit -m "$(cat <<'EOF'
feat(claude-app-server): map Edit/Write/MultiEdit tool_use to fileChange

Phase 3 step 4. Builds simplified diff representation (< old / > new
joined by --- for MultiEdit). Write is always kind:create; Edit and
MultiEdit are kind:edit. unified-diff generation deferred to a later
phase per spec.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: `tool-item.ts` の mcpToolCall 経路

**Files:**
- Modify: `apps/claude-app-server/src/claude/tool-item.ts`
- Modify: `apps/claude-app-server/test/claude/tool-item.test.ts`

- [ ] **Step 1: failing tests を追加**

```typescript
describe("ToolItem.buildStarted (mcpToolCall)", () => {
  it("wraps Read into mcpToolCall with claude-builtin server", () => {
    const itemId = ItemId.fresh();
    const item = ToolItem.buildStarted({
      itemId,
      name: "Read",
      input: { file_path: "/x.ts" },
      cwd: "/tmp",
    });
    expect(item).toMatchObject({
      id: itemId,
      type: "mcpToolCall",
      server: "claude-builtin",
      tool: "Read",
      arguments: { file_path: "/x.ts" },
      status: "inProgress",
    });
  });

  it("wraps unknown tool names into mcpToolCall", () => {
    const itemId = ItemId.fresh();
    const item = ToolItem.buildStarted({
      itemId,
      name: "SomeFutureTool",
      input: { x: 1 },
      cwd: "/tmp",
    });
    expect(item).toMatchObject({
      type: "mcpToolCall",
      tool: "SomeFutureTool",
      arguments: { x: 1 },
    });
  });
});

describe("ToolItem.buildCompleted (mcpToolCall)", () => {
  it("sets result and status:completed on success", () => {
    const itemId = ItemId.fresh();
    const started = ToolItem.buildStarted({
      itemId,
      name: "Read",
      input: { file_path: "/x" },
      cwd: "/tmp",
    });
    const completed = ToolItem.buildCompleted({
      started,
      itemType: "mcpToolCall",
      content: "file contents",
      isError: false,
    });
    expect(completed).toMatchObject({
      type: "mcpToolCall",
      status: "completed",
      result: "file contents",
    });
  });

  it("sets status:failed on is_error", () => {
    const itemId = ItemId.fresh();
    const started = ToolItem.buildStarted({
      itemId,
      name: "Glob",
      input: { pattern: "*.ts" },
      cwd: "/tmp",
    });
    const completed = ToolItem.buildCompleted({
      started,
      itemType: "mcpToolCall",
      content: "not found",
      isError: true,
    });
    expect((completed as { status: string }).status).toBe("failed");
  });
});
```

- [ ] **Step 2: fail を確認**

Run: `cd apps/claude-app-server && pnpm test -- tool-item`
Expected: `Error: mcpToolCall not implemented in this task`。

- [ ] **Step 3: mcpToolCall 経路を実装**

`buildStarted` の `case "mcpToolCall"` を置換:

```typescript
case "mcpToolCall":
  return {
    id: args.itemId,
    type: "mcpToolCall",
    server: "claude-builtin",
    tool: args.name,
    arguments: args.input,
    status: "inProgress",
  };
```

`buildCompleted` の `case "mcpToolCall"`:

```typescript
case "mcpToolCall":
  return {
    ...args.started,
    status: args.isError ? "failed" : "completed",
    result: args.content,
  };
```

- [ ] **Step 4: テスト全体 + lint + format**

Run: `cd apps/claude-app-server && pnpm test && pnpm lint && pnpm format:check`
Expected: 全 PASS。

- [ ] **Step 5: Commit**

```bash
git add apps/claude-app-server/src/claude/tool-item.ts \
        apps/claude-app-server/test/claude/tool-item.test.ts
git commit -m "$(cat <<'EOF'
feat(claude-app-server): wrap remaining tools as mcpToolCall items

Phase 3 step 5. Read / Glob / Grep / WebFetch / etc. are surfaced as
mcpToolCall items with server set to claude-builtin (placeholder, since
Claude built-in tools have no MCP server concept).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `EventMapper.MapperState` 拡張 + `AssistantToolUse` 遷移

**Files:**
- Modify: `apps/claude-app-server/src/claude/event-mapper.ts`
- Modify: `apps/claude-app-server/test/claude/event-mapper.test.ts`

- [ ] **Step 1: failing tests を追加**

`test/claude/event-mapper.test.ts` の末尾に追加:

```typescript
import { ToolItem } from "../../src/claude/tool-item.js";  // 既に import 済みなら不要

describe("EventMapper.map - tool lifecycle", () => {
  it("opens commandExecution item on Bash tool_use, closing in-progress agentMessage", () => {
    const opened = EventMapper.map(
      { kind: "AssistantText", text: "running", sessionId: "s" },
      EventMapper.initialState(),
      ctx,
    );
    const next = EventMapper.map(
      {
        kind: "AssistantToolUse",
        toolUseId: "tu_1",
        name: "Bash",
        input: { command: "ls" },
        sessionId: "s",
      },
      opened.state,
      ctx,
    );
    expect(next.notifications.map((n) => n.method)).toEqual(["item/completed", "item/started"]);
    const startedParams = next.notifications[1]?.params as { item: { type: string; id: string } };
    expect(startedParams.item.type).toBe("commandExecution");
    expect(next.state.pendingTools.size).toBe(1);
    expect(next.state.pendingTools.get("tu_1")?.itemType).toBe("commandExecution");
    expect(next.state.currentAgentMessage).toBeUndefined();
  });

  it("opens 2 pending entries for two tool_use in one assistant message", () => {
    const state = EventMapper.initialState();
    const r1 = EventMapper.map(
      { kind: "AssistantToolUse", toolUseId: "tu_1", name: "Read", input: { file_path: "/a" }, sessionId: "s" },
      state,
      ctx,
    );
    const r2 = EventMapper.map(
      { kind: "AssistantToolUse", toolUseId: "tu_2", name: "Bash", input: { command: "ls" }, sessionId: "s" },
      r1.state,
      ctx,
    );
    expect(r2.state.pendingTools.size).toBe(2);
    expect(r2.state.pendingTools.get("tu_1")?.itemType).toBe("mcpToolCall");
    expect(r2.state.pendingTools.get("tu_2")?.itemType).toBe("commandExecution");
  });

  it("uses thread cwd when building commandExecution started", () => {
    // ctx is fixed; cwd is taken from EventMapper context (need to add cwd to MapperCtx)
    const ctxWithCwd = { ...ctx, cwd: "/my/ws" };
    const r = EventMapper.map(
      { kind: "AssistantToolUse", toolUseId: "tu_1", name: "Bash", input: { command: "x" }, sessionId: "s" },
      EventMapper.initialState(),
      ctxWithCwd,
    );
    const startedParams = r.notifications[0]?.params as { item: { cwd: string } };
    expect(startedParams.item.cwd).toBe("/my/ws");
  });
});
```

- [ ] **Step 2: fail を確認**

Run: `cd apps/claude-app-server && pnpm test -- event-mapper`
Expected: `pendingTools` 未定義、`AssistantToolUse` ブランチが既存実装 (`closeCurrent`) のままで `item/started` が出ないため失敗。

- [ ] **Step 3: `MapperState` 拡張 + `MapperCtx` に cwd 追加 + AssistantToolUse 実装**

`src/claude/event-mapper.ts` を以下のように変更:

```typescript
import { ToolItem, type ToolItemType } from "./tool-item.js";

export type PendingTool = Readonly<{
  itemId: ItemIdT;
  itemType: ToolItemType;
  startedItem: Readonly<Record<string, unknown>>;
}>;

export type MapperState = Readonly<{
  currentAgentMessage?: Readonly<{ itemId: ItemIdT; text: string }>;
  pendingTools: ReadonlyMap<string, PendingTool>;
}>;

export type MapperCtx = Readonly<{
  threadId: ThreadId;
  turnId: TurnId;
  cwd: string;
}>;
```

`initialState()`:

```typescript
initialState: (): MapperState => ({ pendingTools: new Map() }),
```

`map` の `case "AssistantToolUse"` を以下に置き換え:

```typescript
case "AssistantToolUse": {
  const closed = closeCurrent(state, ctx);
  const itemId = ItemId.fresh();
  const startedItem = ToolItem.buildStarted({
    itemId,
    name: msg.name,
    input: msg.input,
    cwd: ctx.cwd,
  });
  const started = OutgoingNotificationM.of("item/started", {
    threadId: ctx.threadId,
    turnId: ctx.turnId,
    item: startedItem,
  });
  const newPending = new Map(closed.state.pendingTools);
  newPending.set(msg.toolUseId, {
    itemId,
    itemType: startedItem.type as ToolItemType,
    startedItem,
  });
  return {
    state: { ...closed.state, pendingTools: newPending },
    notifications: [...closed.notifications, started],
  };
}
```

既存の `closeCurrent` が `state` を `{ currentAgentMessage: undefined }` で返すので `pendingTools` がドロップする問題に注意 — `closeCurrent` 内部を `{ ...state, currentAgentMessage: undefined }` に直す:

```typescript
const closeCurrent = (
  state: MapperState,
  ctx: MapperCtx,
): { state: MapperState; notifications: OutgoingNotification[] } => {
  if (!state.currentAgentMessage) return { state, notifications: [] };
  const closed = itemCompleted(ctx, {
    id: state.currentAgentMessage.itemId,
    type: "agentMessage",
    text: state.currentAgentMessage.text,
  });
  return {
    state: { ...state, currentAgentMessage: undefined },
    notifications: [closed],
  };
};
```

`openAgentMessage` 内部も `state` をマージするように直す:

```typescript
const openAgentMessage = (
  state: MapperState,
  ctx: MapperCtx,
  text: string,
): { state: MapperState; notifications: OutgoingNotification[] } => {
  const itemId = ItemId.fresh();
  return {
    state: { ...state, currentAgentMessage: { itemId, text } },
    notifications: [
      itemStarted(ctx, { id: itemId, type: "agentMessage", text: "" }),
      agentMessageDelta(ctx, itemId, text),
    ],
  };
};
```

`map` の `case "AssistantText"` も `openAgentMessage(closed.state, ctx, msg.text)` を呼ぶように直す。

- [ ] **Step 4: 既存テストのコンパイル追従**

既存 `event-mapper.test.ts` の `ctx` 定義に `cwd: "/tmp"` を追加:

```typescript
const ctx = {
  threadId: ThreadId.fresh(),
  turnId: TurnId.fresh(),
  cwd: "/tmp",
};
```

`session.ts` の `mapCtx` 構築も `cwd: thread.options.cwd` に対応 (現在 `{ threadId, turnId }` だけ):

```typescript
const mapCtx = { threadId: thread.id, turnId: args.turnId, cwd: thread.options.cwd };
```

- [ ] **Step 5: テスト + lint + format + build**

Run: `cd apps/claude-app-server && pnpm test && pnpm lint && pnpm format:check && pnpm build`
Expected: 全 PASS。

- [ ] **Step 6: Commit**

```bash
git add apps/claude-app-server/src/claude/event-mapper.ts \
        apps/claude-app-server/src/claude/session.ts \
        apps/claude-app-server/test/claude/event-mapper.test.ts
git commit -m "$(cat <<'EOF'
feat(claude-app-server): emit item/started on AssistantToolUse, track pending tools

Phase 3 step 6. MapperState gains a pendingTools map keyed by
tool_use_id. AssistantToolUse closes any open agentMessage then opens
a tool item via ToolItem.buildStarted. MapperCtx now carries cwd so
commandExecution items have the right working directory.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: `UserToolResult` 受信 + 未登録 tool_result 黙殺

**Files:**
- Modify: `apps/claude-app-server/src/claude/event-mapper.ts`
- Modify: `apps/claude-app-server/test/claude/event-mapper.test.ts`

- [ ] **Step 1: failing tests を追加**

```typescript
describe("EventMapper.map - UserToolResult", () => {
  it("closes the matching pending tool item with status:completed", () => {
    const startState = EventMapper.map(
      {
        kind: "AssistantToolUse",
        toolUseId: "tu_1",
        name: "Bash",
        input: { command: "ls" },
        sessionId: "s",
      },
      EventMapper.initialState(),
      ctx,
    );
    const next = EventMapper.map(
      { kind: "UserToolResult", toolUseId: "tu_1", content: "file1\nfile2\n", sessionId: "s" },
      startState.state,
      ctx,
    );
    expect(next.notifications.map((n) => n.method)).toEqual(["item/completed"]);
    const params = next.notifications[0]?.params as { item: { status: string; aggregatedOutput: string } };
    expect(params.item.status).toBe("completed");
    expect(params.item.aggregatedOutput).toBe("file1\nfile2\n");
    expect(next.state.pendingTools.size).toBe(0);
  });

  it("sets status:failed when tool_result.is_error indicator is present", () => {
    const startState = EventMapper.map(
      { kind: "AssistantToolUse", toolUseId: "tu_1", name: "Bash", input: { command: "false" }, sessionId: "s" },
      EventMapper.initialState(),
      ctx,
    );
    const next = EventMapper.map(
      { kind: "UserToolResult", toolUseId: "tu_1", content: [{ type: "text", text: "boom" }], sessionId: "s", isError: true },
      startState.state,
      ctx,
    );
    const params = next.notifications[0]?.params as { item: { status: string } };
    expect(params.item.status).toBe("failed");
  });

  it("silently drops tool_result for an unknown tool_use_id", () => {
    const next = EventMapper.map(
      { kind: "UserToolResult", toolUseId: "ghost", content: "x", sessionId: "s" },
      EventMapper.initialState(),
      ctx,
    );
    expect(next.notifications).toEqual([]);
    expect(next.state.pendingTools.size).toBe(0);
  });
});
```

- [ ] **Step 2: `sdk-message.ts` の `UserToolResult` に `isError` フィールドを追加**

`ToolResultBlockSchema` を以下のように拡張:

```typescript
const ToolResultBlockSchema = v.object({
  type: v.literal("tool_result"),
  tool_use_id: v.string(),
  content: v.unknown(),
  is_error: v.optional(v.boolean()),
});
```

DU バリアントを更新:

```typescript
| Readonly<{
    kind: "UserToolResult";
    toolUseId: string;
    content: unknown;
    isError: boolean;
    sessionId: string;
  }>
```

`parseUserBlocks` 内部:

```typescript
out.push({
  kind: "UserToolResult",
  toolUseId: tr.output.tool_use_id,
  content: tr.output.content,
  isError: tr.output.is_error ?? false,
  sessionId: r.output.session_id,
});
```

- [ ] **Step 3: `event-mapper.ts` の `case "UserToolResult"` を実装**

```typescript
case "UserToolResult": {
  const pending = state.pendingTools.get(msg.toolUseId);
  if (!pending) return { state, notifications: [] };
  const contentStr = ToolItem.normalizeToolResultContent(msg.content);
  const completedItem = ToolItem.buildCompleted({
    started: pending.startedItem as StartedItem,
    itemType: pending.itemType,
    content: contentStr,
    isError: msg.isError,
  });
  const notification = OutgoingNotificationM.of("item/completed", {
    threadId: ctx.threadId,
    turnId: ctx.turnId,
    item: completedItem,
  });
  const newPending = new Map(state.pendingTools);
  newPending.delete(msg.toolUseId);
  return {
    state: { ...state, pendingTools: newPending },
    notifications: [notification],
  };
}
```

`StartedItem` 型を `event-mapper.ts` に import:

```typescript
import { ToolItem, type StartedItem, type ToolItemType } from "./tool-item.js";
```

- [ ] **Step 4: テスト全体 + lint + format + build**

Run: `cd apps/claude-app-server && pnpm test && pnpm lint && pnpm format:check && pnpm build`
Expected: 全 PASS。

- [ ] **Step 5: Commit**

```bash
git add apps/claude-app-server/src/claude/sdk-message.ts \
        apps/claude-app-server/src/claude/event-mapper.ts \
        apps/claude-app-server/test/claude/event-mapper.test.ts
git commit -m "$(cat <<'EOF'
feat(claude-app-server): close pending tool item on UserToolResult

Phase 3 step 7. Adds is_error to UserToolResult parsing and uses
ToolItem.buildCompleted to emit item/completed with the right status.
tool_results whose tool_use_id was never registered are dropped
silently (defensive: covers Claude SDK quirks).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: `ResultSuccess` / `ResultError` を no-op 化 + `closeAll` 実装

**Files:**
- Modify: `apps/claude-app-server/src/claude/event-mapper.ts`
- Modify: `apps/claude-app-server/test/claude/event-mapper.test.ts`

- [ ] **Step 1: 既存テストを更新**

既存 `event-mapper.test.ts` の以下テストを書き換え:

```typescript
it("emits nothing on ResultSuccess (close is session responsibility)", () => {
  const opened = EventMapper.map(
    { kind: "AssistantText", text: "answer", sessionId: "s" },
    EventMapper.initialState(),
    ctx,
  );
  const result = EventMapper.map(
    { kind: "ResultSuccess", sessionId: "s", resultText: "answer" },
    opened.state,
    ctx,
  );
  expect(result.notifications).toEqual([]);
  expect(result.state).toEqual(opened.state);
});

it("emits nothing on ResultError (close is session responsibility)", () => {
  const opened = EventMapper.map(
    { kind: "AssistantText", text: "x", sessionId: "s" },
    EventMapper.initialState(),
    ctx,
  );
  const result = EventMapper.map(
    { kind: "ResultError", sessionId: "s", subtype: "error_during_execution", errors: ["boom"] },
    opened.state,
    ctx,
  );
  expect(result.notifications).toEqual([]);
});
```

(以前は ResultSuccess/Error で `item/completed` が出る挙動だった。これを「何も出さない」に変える。)

- [ ] **Step 2: failing tests を追加 (`closeAll`)**

```typescript
describe("EventMapper.closeAll", () => {
  it("closes nothing when state is empty", () => {
    const r = EventMapper.closeAll(EventMapper.initialState(), ctx, "completed");
    expect(r.notifications).toEqual([]);
    expect(r.state.pendingTools.size).toBe(0);
  });

  it("closes only the open agentMessage when reason=completed", () => {
    const opened = EventMapper.map(
      { kind: "AssistantText", text: "answer", sessionId: "s" },
      EventMapper.initialState(),
      ctx,
    );
    const r = EventMapper.closeAll(opened.state, ctx, "completed");
    expect(r.notifications).toHaveLength(1);
    const params = r.notifications[0]?.params as { item: { type: string; status: string } };
    expect(params.item.type).toBe("agentMessage");
    expect(params.item.status).toBe("completed");
    expect(r.state.currentAgentMessage).toBeUndefined();
  });

  it("closes pending tool items with status=failed when reason=failed", () => {
    const r1 = EventMapper.map(
      { kind: "AssistantToolUse", toolUseId: "tu_1", name: "Bash", input: { command: "x" }, sessionId: "s" },
      EventMapper.initialState(),
      ctx,
    );
    const r = EventMapper.closeAll(r1.state, ctx, "failed");
    expect(r.notifications).toHaveLength(1);
    const params = r.notifications[0]?.params as { item: { type: string; status: string } };
    expect(params.item.type).toBe("commandExecution");
    expect(params.item.status).toBe("failed");
    expect(r.state.pendingTools.size).toBe(0);
  });

  it("closes both agentMessage and pending tool items with reason=interrupted", () => {
    const s1 = EventMapper.map(
      { kind: "AssistantText", text: "running", sessionId: "s" },
      EventMapper.initialState(),
      ctx,
    );
    const s2 = EventMapper.map(
      { kind: "AssistantToolUse", toolUseId: "tu_1", name: "Bash", input: { command: "x" }, sessionId: "s" },
      s1.state,
      ctx,
    );
    // s2 already closed the agentMessage; add a new agentMessage on top
    const s3 = EventMapper.map(
      { kind: "AssistantText", text: "more", sessionId: "s" },
      s2.state,
      ctx,
    );
    const r = EventMapper.closeAll(s3.state, ctx, "interrupted");
    const statuses = r.notifications.map(
      (n) => (n.params as { item: { status: string } }).item.status,
    );
    expect(statuses).toEqual(["interrupted", "interrupted"]);
    expect(r.state.pendingTools.size).toBe(0);
    expect(r.state.currentAgentMessage).toBeUndefined();
  });
});
```

- [ ] **Step 3: fail を確認**

Run: `cd apps/claude-app-server && pnpm test -- event-mapper`
Expected: `closeAll` 未定義、ResultSuccess/Error テストも fail。

- [ ] **Step 4: 実装**

`src/claude/event-mapper.ts`:

`map` の `case "ResultSuccess"` と `case "ResultError"` を以下に置き換え:

```typescript
case "ResultSuccess":
case "ResultError":
  return { state, notifications: [] };
```

`closeAll` を追加:

```typescript
export type CloseReason = "completed" | "failed" | "interrupted";

const closeAll = (
  state: MapperState,
  ctx: MapperCtx,
  reason: CloseReason,
): MapResult => {
  const notifications: OutgoingNotification[] = [];

  if (state.currentAgentMessage) {
    notifications.push(
      OutgoingNotificationM.of("item/completed", {
        threadId: ctx.threadId,
        turnId: ctx.turnId,
        item: {
          id: state.currentAgentMessage.itemId,
          type: "agentMessage",
          text: state.currentAgentMessage.text,
          status: reason,
        },
      }),
    );
  }

  for (const [, pending] of state.pendingTools) {
    notifications.push(
      OutgoingNotificationM.of("item/completed", {
        threadId: ctx.threadId,
        turnId: ctx.turnId,
        item: { ...pending.startedItem, status: reason },
      }),
    );
  }

  return {
    state: { pendingTools: new Map(), currentAgentMessage: undefined },
    notifications,
  };
};

export const EventMapper = {
  initialState: ...,
  map: ...,
  closeAll,
} as const;
```

- [ ] **Step 5: テスト全体 + lint + format + build**

Run: `cd apps/claude-app-server && pnpm test && pnpm lint && pnpm format:check && pnpm build`
Expected: 全 PASS。

- [ ] **Step 6: Commit**

```bash
git add apps/claude-app-server/src/claude/event-mapper.ts \
        apps/claude-app-server/test/claude/event-mapper.test.ts
git commit -m "$(cat <<'EOF'
feat(claude-app-server): add EventMapper.closeAll, ResultSuccess/Error no-op

Phase 3 step 8. Terminal SdkMessages no longer emit close notifications
from EventMapper.map; session.ts is now responsible for invoking
closeAll(reason) once at the end of each turn. closeAll emits
item/completed for every open agentMessage and pending tool with the
given reason as status.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: `session.ts` を `closeAll` 呼び出しに置換

**Files:**
- Modify: `apps/claude-app-server/src/claude/session.ts`

- [ ] **Step 1: `closeAgentMessageIfAny` を削除し `closeAll` を呼ぶ構造に書き換え**

`src/claude/session.ts` 全体を以下構造に:

```typescript
import { AbortError, query } from "@anthropic-ai/claude-agent-sdk";
import { EventMapper, type CloseReason, type MapperState } from "./event-mapper.js";
import { SdkMessage } from "./sdk-message.js";
import { toSdkOptions } from "./session-options.js";
import { OutgoingNotification } from "../jsonrpc/outgoing-message.js";
import { SessionId, type TurnId } from "../util/ids.js";
import type { Threads, ThreadState } from "../state/threads.js";
import type { ServerOptions } from "../server.js";

// (型定義は既存のまま)

export const Session = {
  create: (deps: SessionDeps): Session => ({
    runTurn: async (args) => {
      const { thread, threads, sendNotification, abortController } = args;

      sendNotification(
        OutgoingNotification.of("turn/started", {
          threadId: thread.id,
          turn: turnSnapshot(args, "inProgress", null),
        }),
      );

      const sdkOpts = toSdkOptions({
        thread: thread.options,
        resume: thread.sessionId,
        abortController,
      });

      let mapperState: MapperState = EventMapper.initialState();
      const mapCtx = { threadId: thread.id, turnId: args.turnId, cwd: thread.options.cwd };

      let status: "completed" | "failed" | "interrupted" = "completed";
      let error: { message: string } | null = null;

      try {
        for await (const raw of deps.query({ prompt: promptFrom(args.input), options: sdkOpts })) {
          for (const parsed of SdkMessage.parse(raw)) {
            if (parsed.kind === "SystemInit") {
              const sid = SessionId.parse(parsed.sessionId);
              if (sid.type === "Success") threads.setSessionId(thread.id, sid.value);
            }
            const r = EventMapper.map(parsed, mapperState, mapCtx);
            mapperState = r.state;
            for (const n of r.notifications) sendNotification(n);

            if (parsed.kind === "ResultError") {
              status = "failed";
              error = { message: parsed.errors.join("; ") || `result error: ${parsed.subtype}` };
            }
          }
        }
      } catch (err) {
        if (err instanceof AbortError || (err instanceof Error && err.name === "AbortError")) {
          status = "interrupted";
        } else {
          status = "failed";
          error = { message: err instanceof Error ? err.message : String(err) };
        }
      } finally {
        const reason: CloseReason = status;  // status の値域は CloseReason と一致
        const closed = EventMapper.closeAll(mapperState, mapCtx, reason);
        for (const n of closed.notifications) sendNotification(n);

        threads.clearCurrentTurn(thread.id);
        sendNotification(
          OutgoingNotification.of("turn/completed", {
            threadId: thread.id,
            turn: turnSnapshot(args, status, error),
          }),
        );
      }
    },
  }),
} as const;
```

`closeAgentMessageIfAny` ヘルパは削除。

- [ ] **Step 2: テスト全体 + lint + format + build**

Run: `cd apps/claude-app-server && pnpm test && pnpm lint && pnpm format:check && pnpm build`
Expected: 全 PASS。session.ts に unit テストはないが、event-mapper の close 動作で間接カバー。

- [ ] **Step 3: Commit**

```bash
git add apps/claude-app-server/src/claude/session.ts
git commit -m "$(cat <<'EOF'
refactor(claude-app-server): replace closeAgentMessageIfAny with EventMapper.closeAll

Phase 3 step 9. Drop the pseudo-ResultError trick from Phase 2. session.ts
now calls EventMapper.closeAll(reason) once in the finally block, with
reason mapped from the turn's terminal status (completed / failed /
interrupted). All open agentMessage and pending tool items are closed
uniformly.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: `examples/manual_test.jsonl` 拡張 + 手動 E2E 確認

**Files:**
- Modify: `apps/claude-app-server/examples/manual_test.jsonl`

- [ ] **Step 1: シナリオを拡張**

`apps/claude-app-server/examples/manual_test.jsonl` を以下に書き換え:

```jsonl
{"method":"initialize","id":0,"params":{"capabilities":{"experimentalApi":true},"clientInfo":{"name":"manual-test","title":"Manual Test","version":"0.1.0"}}}
{"method":"initialized","params":{}}
{"method":"thread/start","id":1,"params":{"cwd":"/tmp"}}
{"method":"turn/start","id":2,"params":{"threadId":"REPLACE_WITH_THREAD_ID","input":[{"type":"text","text":"Create a file at /tmp/cas-phase3-test.txt with the content 'hello phase3', then read it back with the Read tool, then cat it with bash. Report the final contents."}]}}
```

- [ ] **Step 2: 手動 E2E を実行 (人手で代用)**

このタスクのこのステップは agentic worker では実行できないので、コミット時に「手動 E2E が必要」と note を残す:

```bash
# 手動 E2E の実行手順 (担当者が実機で行う):
#   1. cd apps/claude-app-server && pnpm build
#   2. claude --version で OAuth セッション存在確認
#   3. node dist/bin.js --model claude-opus-4-7 --permission-mode bypassPermissions
#   4. examples/manual_test.jsonl を stdin に流す (thread_id は手で書き換える)
#   5. 以下が順に出ること:
#      - turn/started
#      - item/started (fileChange) → item/completed (fileChange, completed)
#      - item/started (mcpToolCall, tool=Read) → item/completed (mcpToolCall, completed)
#      - item/started (commandExecution) → item/completed (commandExecution, completed)
#      - item/started (agentMessage) → item/agentMessage/delta → item/completed (agentMessage, completed)
#      - turn/completed (status: completed)
```

- [ ] **Step 3: Commit**

```bash
git add apps/claude-app-server/examples/manual_test.jsonl
git commit -m "$(cat <<'EOF'
test(claude-app-server): exercise tool mappings in manual_test.jsonl

Phase 3 step 10. Expanded scenario triggers Write + Read + Bash so the
fileChange / mcpToolCall / commandExecution paths all run end-to-end
during manual smoke testing.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: README + Codex 本家との差異セクション

**Files:**
- Create: `apps/claude-app-server/README.md`

- [ ] **Step 1: README を新規作成**

`apps/claude-app-server/README.md`:

````markdown
# `claude-app-server`

Codex App Server 互換の JSON-RPC 2.0 stdio サーバを Claude Agent SDK バックエンドで提供する。`apps/symphony` から subprocess として起動され、Symphony を改変せず Claude バックエンドで動かすことが目的。設計の詳細は [`../../CLAUDE.md`](../../CLAUDE.md) と [`docs/protocol.md`](../../docs/protocol.md) を参照。

## 起動方法

```bash
claude-app-server --model claude-opus-4-7 --permission-mode bypassPermissions
```

CLI 引数:

- `--model <id>` Claude モデル ID (例: `claude-opus-4-7`)
- `--permission-mode <mode>` `default` / `acceptEdits` / `bypassPermissions`
- `--allowed-tools <list>` 許可するツール名 (カンマ区切り)
- `--cwd <path>` 既定 cwd (`thread/start.params.cwd` で上書き可能)

`thread/start.params` が後勝ち。

## 認証

Claude Pro/Max サブスクリプション (OAuth) 前提。起動時に `ANTHROPIC_API_KEY` 環境変数があってもクリアして CLI の `~/.claude/` を必ず使う。

## 対応プロトコル

`docs/protocol.md` の subset を実装:

- `initialize` / `initialized`
- `thread/start`
- `turn/start` / `turn/interrupt`

未実装メソッドは `-32601 Method not found` を返す。

## Codex App Server 本家との既知の差異

| 項目 | Codex 本家 | claude-app-server | 理由 |
|---|---|---|---|
| `item/commandExecution/outputDelta` | bash 実行中に stdout/stderr を逐次配信 | 出さない。`item/completed.aggregatedOutput` に最終結果のみ | Claude Agent SDK が tool 実行中の段階的出力を露出しない |
| `item/fileChange/outputDelta` | 編集中の差分を逐次配信 | 出さない | 同上 |
| `commandExecution.exitCode` | プロセスの実 exit code | 一律 `null` (`is_error: true` で `status: "failed"` 判別) | Claude SDK が exit code を返さない |
| `commandExecution.durationMs` | 実時間計測 | 一律 `null` | Phase 3 では計測しない |
| `fileChange.changes[*].diff` | unified diff | 簡易表現 (`< old\n> new`、MultiEdit は `\n---\n` 区切り) | Phase 3 工数都合。将来改善余地 |
| `fileChange.changes[*].kind` | edit/create/delete を正確判定 | Edit/MultiEdit → `edit`、Write → 一律 `create` | Claude SDK 上で新規/既存を判別不能 |
| `mcpToolCall.server` | 実 MCP server 名 | `"claude-builtin"` 固定 | Claude builtin tool に MCP server 概念がない |
| `thread/tokenUsage/updated` | 都度発行 | 未発行 | Phase 4 で対応予定 |
| 未実装メソッド | (Codex 互換すべて実装) | `-32601 Method not found` | Symphony が使う subset のみサポート |

Symphony と組み合わせる限り、上記の差異はいずれも実害なし (Symphony はこれらに依存しない)。Codex App Server を別クライアントから直接叩く場合は本表を参照のこと。

## 開発

```bash
pnpm install
pnpm test          # vitest
pnpm build         # tsc + chmod +x dist/bin.js
pnpm lint          # oxlint
pnpm format        # oxfmt
```

詳細は [`CLAUDE.md`](./CLAUDE.md) を参照。
````

- [ ] **Step 2: lint / format (README はチェック対象外だがリポジトリ整合確認)**

Run: `cd apps/claude-app-server && pnpm test && pnpm build`
Expected: PASS。

- [ ] **Step 3: Commit**

```bash
git add apps/claude-app-server/README.md
git commit -m "$(cat <<'EOF'
docs(claude-app-server): add README with Codex App Server divergence table

Phase 3 step 11. Documents the public surface (CLI flags, supported
JSON-RPC methods) and the known differences from Codex App Server
(no outputDelta, simplified fileChange.diff, exitCode/durationMs null,
mcpToolCall.server constant, etc.) so downstream users are not
surprised.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: TODO.md チェック更新 + 最終検証

**Files:**
- Modify: `TODO.md` (リポジトリルート)

- [ ] **Step 1: `TODO.md` の Phase 3 セクションのチェックボックスを更新**

`TODO.md` の Phase 3 セクション (現在 `[ ]` の 6 行) を以下に書き換え:

```markdown
### Phase 3: ツール呼び出しのマッピング

**完了済み（2026-05-12、`feat/cas-phase3` ブランチ）。** tool_use / tool_result マッピング、closeAll による pending クローズ、Codex 本家との差異 README 化。

- [x] `tool_use(Bash)` → `commandExecution` item
- [x] `tool_use(Edit/Write/MultiEdit)` → `fileChange` item
- [x] `tool_use(Read/Glob/Grep その他)` → 最小限の item (`mcpToolCall` ラッパー)
- [x] `tool_result` → 対応 item を `item/completed` に
- [ ] `outputDelta` 系ストリーミング通知 (Phase 4: Claude SDK の制約で本家と差異あり、README に明記)
- [x] `turn/interrupt` 実装（`AbortController`、Phase 2 で導入、Phase 3 で tool 進行中の close 動作確認）
```

`outputDelta` は出さない方針なので Phase 4 マークのまま残す。

- [ ] **Step 2: 最終全体検証**

Run:
```bash
cd apps/claude-app-server && pnpm test && pnpm build && pnpm lint && pnpm format:check
```
Expected: 全 PASS。テスト件数は Phase 2 完了時点 97 から +30 程度 (sdk-message +5、tool-item +14、event-mapper +9 程度の見込み)。

- [ ] **Step 3: Commit**

```bash
git add TODO.md
git commit -m "$(cat <<'EOF'
docs: mark claude-app-server Phase 3 tasks complete

outputDelta stays unchecked: intentionally deferred per Codex divergence
documented in apps/claude-app-server/README.md.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: ブランチを main にマージする提案をユーザに行う**

実装完了後、以下をユーザに提示:
- 手動 E2E (Task 10 Step 2) が未実施なら実施を依頼
- `git log main..feat/cas-phase3 --oneline` でコミット列を確認
- `git merge --no-ff feat/cas-phase3` で main に取り込む案内

(マージ自体はユーザ承認後に実行。`finishing-a-development-branch` skill を使って選択肢を提示するのが望ましい。)
