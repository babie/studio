# claude-app-server Phase 4 (削減版) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `apps/claude-app-server` に stub ハンドラ、レート制限ハンドリング (`UsageLimitExceeded`)、Codex 由来フィールド無視の回帰テスト、README 更新を追加し、Phase 4 (削減版) を完了する。

**Architecture:** Codex App Server 互換 JSON-RPC stdio サーバの仕上げ。stub.ts に未実装メソッドの拡張可能フレームを置き、SdkMessage に `RateLimit` kind を追加して Session で usage limit を検知し `turn/completed.turn.error.codexErrorInfo: "UsageLimitExceeded"` で抜ける。Codex 由来フィールド (approvalPolicy 等) は既存の looseObject 寛容パースを回帰テストで固定。

**Tech Stack:** TypeScript (ESM, NodeNext), vitest, valibot, @praha/byethrow, @anthropic-ai/claude-agent-sdk, oxlint + oxfmt

**仕様:** [`docs/superpowers/specs/2026-05-12-cas-phase4-design.md`](../specs/2026-05-12-cas-phase4-design.md)

**作業ディレクトリ:** すべてのコマンドは `apps/claude-app-server/` 配下で実行。プロジェクトルートは `/Users/babie/src/github.com/babie/concert`。

---

## ファイル構成

### 新規作成
- `apps/claude-app-server/src/handlers/stub.ts` — `registerStub` helper と `registerStubs` 集約関数
- `apps/claude-app-server/test/handlers/stub.test.ts` — stub のテスト
- `apps/claude-app-server/test/claude/session.test.ts` — Session.runTurn の単体テスト (RateLimit / codexErrorInfo 検証)

### 変更
- `apps/claude-app-server/src/handlers/index.ts` — `registerStubs(dispatcher)` 呼び出しを追加
- `apps/claude-app-server/src/claude/sdk-message.ts` — `RateLimit` kind と schema 追加
- `apps/claude-app-server/src/claude/session.ts` — `usageLimitHit` フラグ + `turn.error.codexErrorInfo` 反映、フォールバックのテキストマッチ判定
- `apps/claude-app-server/test/claude/sdk-message.test.ts` — `RateLimit` パーステスト追加
- `apps/claude-app-server/test/handlers/thread.test.ts` — Codex 由来フィールド無視テストを補強
- `apps/claude-app-server/test/handlers/turn.test.ts` — `turn/start` でも Codex 由来フィールド無視を確認
- `apps/claude-app-server/README.md` — 「既知の差異」テーブル更新、「未実装メソッド」「レート制限」セクション追加

---

### Task 1: stub ハンドラの実装

`registerStub` helper と `mcpServerStatus/list` のスタブを登録する仕組みを TDD で作る。

**Files:**
- Create: `apps/claude-app-server/src/handlers/stub.ts`
- Create: `apps/claude-app-server/test/handlers/stub.test.ts`
- Modify: `apps/claude-app-server/src/handlers/index.ts`

- [ ] **Step 1: Write the failing test (stub.test.ts)**

Create `apps/claude-app-server/test/handlers/stub.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { Dispatcher, type HandlerContext } from "../../src/jsonrpc/dispatcher.js";
import { Threads } from "../../src/state/threads.js";
import { registerStub, registerStubs } from "../../src/handlers/stub.js";
import type { Session } from "../../src/claude/session.js";

const makeCtx = (): HandlerContext => ({
  sendNotification: vi.fn(),
  cli: {},
  threads: Threads.create(),
  session: { runTurn: vi.fn() } satisfies Session,
});

describe("registerStubs", () => {
  it("answers mcpServerStatus/list with { servers: [] }", async () => {
    const d = Dispatcher.create();
    registerStubs(d);
    const res = await d.dispatch(
      { kind: "Request", id: 1 as never, method: "mcpServerStatus/list", params: {} },
      makeCtx(),
    );
    expect(res?.kind).toBe("Success");
    if (res?.kind === "Success") {
      expect(res.result).toEqual({ servers: [] });
    }
  });

  it("leaves other unregistered methods to dispatcher default (-32601)", async () => {
    const d = Dispatcher.create();
    registerStubs(d);
    const res = await d.dispatch(
      { kind: "Request", id: 2 as never, method: "account/login", params: {} },
      makeCtx(),
    );
    expect(res?.kind).toBe("Error");
    if (res?.kind === "Error") {
      expect(res.error.code).toBe(-32601);
    }
  });
});

describe("registerStub", () => {
  it("registers an arbitrary method with a static response", async () => {
    const d = Dispatcher.create();
    registerStub(d, "thread/resume", { thread: { id: "thr_dummy" } });
    const res = await d.dispatch(
      { kind: "Request", id: 3 as never, method: "thread/resume", params: {} },
      makeCtx(),
    );
    expect(res?.kind).toBe("Success");
    if (res?.kind === "Success") {
      expect(res.result).toEqual({ thread: { id: "thr_dummy" } });
    }
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd apps/claude-app-server
pnpm test -- test/handlers/stub.test.ts
```

Expected: FAIL (`Cannot find module '../../src/handlers/stub.js'`)

- [ ] **Step 3: Implement stub.ts**

Create `apps/claude-app-server/src/handlers/stub.ts`:

```ts
import type { Dispatcher } from "../jsonrpc/dispatcher.js";

/**
 * 任意のメソッドに静的レスポンスのスタブを登録する。
 * 擬似応答を増やしたい場合は registerStubs 内に行を足す。
 */
export const registerStub = (
  dispatcher: Dispatcher,
  method: string,
  response: unknown,
): void => {
  dispatcher.registerRequest(method, async () => response);
};

/**
 * Phase 4 時点のスタブ群を集約する。
 *
 * - `mcpServerStatus/list` → 空 servers リスト
 * - その他の未実装メソッド (`account/*`, `fs/*`, `thread/fork`, `thread/list`,
 *   `turn/steer`, `command/exec`, `thread/resume` 等) は dispatcher のデフォルト
 *   `-32601 Method not found` に任せる。擬似応答が必要になったらここに
 *   `registerStub(...)` を足す。
 */
export const registerStubs = (dispatcher: Dispatcher): void => {
  registerStub(dispatcher, "mcpServerStatus/list", { servers: [] });
};
```

- [ ] **Step 4: Wire it into handlers/index.ts**

Modify `apps/claude-app-server/src/handlers/index.ts`:

```ts
import type { Dispatcher } from "../jsonrpc/dispatcher.js";
import { initializeHandler, initializedHandler } from "./initialize.js";
import { registerStubs } from "./stub.js";
import { registerThreadHandlers } from "./thread.js";
import { registerTurnHandlers } from "./turn.js";

export const registerHandlers = (dispatcher: Dispatcher): void => {
  dispatcher.registerRequest("initialize", initializeHandler);
  dispatcher.registerNotification("initialized", initializedHandler);
  registerThreadHandlers(dispatcher);
  registerTurnHandlers(dispatcher);
  registerStubs(dispatcher);
};
```

- [ ] **Step 5: Run test to verify it passes**

```bash
pnpm test -- test/handlers/stub.test.ts
```

Expected: PASS (3 tests)

- [ ] **Step 6: Commit**

```bash
git add apps/claude-app-server/src/handlers/stub.ts \
        apps/claude-app-server/src/handlers/index.ts \
        apps/claude-app-server/test/handlers/stub.test.ts
git commit -m "feat(claude-app-server): add stub handler framework + mcpServerStatus/list

registerStub helper で未実装メソッドの擬似応答を後付け可能な構造にし、
mcpServerStatus/list は空 servers リストで応答するスタブを登録。
他の未実装メソッド (account/*, fs/*, thread/fork, thread/list, turn/steer,
command/exec, thread/resume 等) は dispatcher のデフォルト -32601 に任せる。"
```

---

### Task 2: Codex 由来フィールド無視テストの補強

`thread.test.ts` には既に `ignores unknown params fields` テストがあるが、Phase 4 で「固定する」ため強化し、`turn.test.ts` にも同等のテストを追加する。

**Files:**
- Modify: `apps/claude-app-server/test/handlers/thread.test.ts`
- Modify: `apps/claude-app-server/test/handlers/turn.test.ts`

- [ ] **Step 1: 既存 thread.test.ts のテストを強化**

`thread.test.ts` の `it("ignores unknown params fields", ...)` (63〜71 行) を、Symphony が実際に乗せてくる Codex 由来フィールド全てを入れた厚いケースに置き換える:

```ts
  it("ignores Codex-origin and unknown params fields (approvalPolicy, sandboxPolicy.writableRoots, networkAccess, effort, etc.)", async () => {
    const { res } = await dispatch({
      cwd: "/w",
      model: "claude-opus-4-7",
      approvalPolicy: "never",
      sandboxPolicy: {
        type: "workspaceWrite",
        writableRoots: ["/tmp", "/work"],
        networkAccess: true,
      },
      effort: "low",
      bogus: true,
    });
    expect(res?.kind).toBe("Success");
  });
```

- [ ] **Step 2: turn.test.ts にも同等のテストを追加**

`turn.test.ts` の `describe("turn/start", ...)` の末尾 (`it("rejects missing input ...")` の前) に追加:

```ts
  it("ignores Codex-origin and unknown params fields on turn/start", async () => {
    const d = Dispatcher.create();
    registerThreadHandlers(d);
    registerTurnHandlers(d);
    const { ctx } = makeCtx();
    const threadId = await startThread(d, ctx);

    const res = await d.dispatch(
      {
        kind: "Request",
        id: 2 as never,
        method: "turn/start",
        params: {
          threadId,
          input: [{ type: "text", text: "hi" }],
          approvalPolicy: "never",
          sandboxPolicy: { type: "workspaceWrite", writableRoots: ["/tmp"], networkAccess: true },
          effort: "low",
          bogus: 42,
        },
      },
      ctx,
    );
    expect(res?.kind).toBe("Success");
  });
```

- [ ] **Step 3: Run tests to verify they pass (regression fixation)**

```bash
pnpm test -- test/handlers/thread.test.ts test/handlers/turn.test.ts
```

Expected: PASS (`thread.test` は既存 5 → 5 件、`turn.test` は既存 7 → 8 件)

- [ ] **Step 4: Commit**

```bash
git add apps/claude-app-server/test/handlers/thread.test.ts \
        apps/claude-app-server/test/handlers/turn.test.ts
git commit -m "test(claude-app-server): fix Codex-origin field tolerance on thread/turn start

Symphony が thread/start / turn/start に乗せてくる Codex 由来の不明フィールド
(approvalPolicy / sandboxPolicy.writableRoots / networkAccess / effort 等) が
looseObject 寛容パースで無視されることを回帰テストで固定。"
```

---

### Task 3: SdkMessage に RateLimit kind を追加

Claude Agent SDK の `rate_limit_event` を `kind: "RateLimit"` にパースする。`rate_limit_info.status` か `overageStatus` のいずれかが `"rejected"` なら `status: "rejected"` を伝播。

**Files:**
- Modify: `apps/claude-app-server/src/claude/sdk-message.ts`
- Modify: `apps/claude-app-server/test/claude/sdk-message.test.ts`

- [ ] **Step 1: Write failing tests (sdk-message.test.ts)**

`test/claude/sdk-message.test.ts` の `describe("SdkMessage.parse", ...)` 内の末尾 (`it("skips unknown content blocks ...")` の後) に追加:

```ts
  it("parses rate_limit_event (status: allowed) into RateLimit", () => {
    const parsed = SdkMessage.parse({
      type: "rate_limit_event",
      rate_limit_info: { status: "allowed", utilization: 0.1 },
      session_id: "s",
      uuid: "u",
    });
    expect(parsed).toHaveLength(1);
    const msg = parsed[0];
    expect(msg?.kind).toBe("RateLimit");
    if (msg?.kind === "RateLimit") {
      expect(msg.status).toBe("allowed");
    }
  });

  it("parses rate_limit_event with rate_limit_info.status: rejected as rejected", () => {
    const parsed = SdkMessage.parse({
      type: "rate_limit_event",
      rate_limit_info: { status: "rejected", rateLimitType: "five_hour" },
      session_id: "s",
      uuid: "u",
    });
    expect(parsed).toHaveLength(1);
    const msg = parsed[0];
    expect(msg?.kind).toBe("RateLimit");
    if (msg?.kind === "RateLimit") {
      expect(msg.status).toBe("rejected");
    }
  });

  it("parses rate_limit_event with overageStatus: rejected as rejected even if status is allowed", () => {
    const parsed = SdkMessage.parse({
      type: "rate_limit_event",
      rate_limit_info: { status: "allowed", overageStatus: "rejected" },
      session_id: "s",
      uuid: "u",
    });
    expect(parsed).toHaveLength(1);
    const msg = parsed[0];
    expect(msg?.kind).toBe("RateLimit");
    if (msg?.kind === "RateLimit") {
      expect(msg.status).toBe("rejected");
    }
  });

  it("defaults RateLimit status to 'allowed' when rate_limit_info.status is missing", () => {
    const parsed = SdkMessage.parse({
      type: "rate_limit_event",
      rate_limit_info: {},
      session_id: "s",
      uuid: "u",
    });
    expect(parsed).toHaveLength(1);
    const msg = parsed[0];
    expect(msg?.kind).toBe("RateLimit");
    if (msg?.kind === "RateLimit") {
      expect(msg.status).toBe("allowed");
    }
  });
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
pnpm test -- test/claude/sdk-message.test.ts
```

Expected: FAIL (4 tests fail — kind が `"Unknown"` のため)

- [ ] **Step 3: Implement RateLimit kind in sdk-message.ts**

`src/claude/sdk-message.ts` の編集箇所:

`v.picklist` の import は既に valibot に含まれる。`SystemInitSchema` 等が並んでいる箇所 (`ResultErrorSubtypeSchema` の後あたり) に追加:

```ts
const RateLimitStatusSchema = v.picklist(["allowed", "allowed_warning", "rejected"]);

const RateLimitInfoSchema = v.looseObject({
  status: v.optional(RateLimitStatusSchema),
  overageStatus: v.optional(RateLimitStatusSchema),
});

const RateLimitEventSchema = v.object({
  type: v.literal("rate_limit_event"),
  rate_limit_info: RateLimitInfoSchema,
  session_id: v.string(),
});
```

`export type SdkMessage = ... | Readonly<{ kind: "Unknown"; raw: unknown }>;` の直前に新 kind を挿入:

```ts
  | Readonly<{
      kind: "RateLimit";
      status: "allowed" | "allowed_warning" | "rejected";
      sessionId: string;
    }>
```

`SdkMessage.parse` 関数の中、`const resErr = v.safeParse(ResultErrorSchema, raw);` のあとに、assistant 試行の前に RateLimit 判定を入れる:

```ts
    const rl = v.safeParse(RateLimitEventSchema, raw);
    if (rl.success) {
      const info = rl.output.rate_limit_info;
      const rejected = info.status === "rejected" || info.overageStatus === "rejected";
      const status = rejected ? "rejected" : (info.status ?? "allowed");
      return [{ kind: "RateLimit", status, sessionId: rl.output.session_id }];
    }
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pnpm test -- test/claude/sdk-message.test.ts
```

Expected: PASS (4 new tests + 既存 13 件)

- [ ] **Step 5: Commit**

```bash
git add apps/claude-app-server/src/claude/sdk-message.ts \
        apps/claude-app-server/test/claude/sdk-message.test.ts
git commit -m "feat(claude-app-server): parse rate_limit_event into RateLimit kind

Claude Agent SDK の rate_limit_event を SdkMessage の RateLimit kind に
パースする。rate_limit_info.status または overageStatus のどちらかが
'rejected' なら status: 'rejected' を伝播。後続 Session が usage limit
判定に使う。"
```

---

### Task 4: Session の usage limit 検知と turn.error 拡張

`Session.runTurn` 内で `RateLimit (rejected)` を見たら `usageLimitHit` フラグを立て、終了処理時に `turn/completed.turn.error.codexErrorInfo = "UsageLimitExceeded"` を載せる。フラグが立っていなくても、例外 / errors テキストに rate limit パターンが含まれていれば同等扱い。

**Files:**
- Modify: `apps/claude-app-server/src/claude/session.ts`
- Create: `apps/claude-app-server/test/claude/session.test.ts`

- [ ] **Step 1: Write failing tests (session.test.ts)**

Create `apps/claude-app-server/test/claude/session.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { Session, type RunTurnArgs } from "../../src/claude/session.js";
import { Threads } from "../../src/state/threads.js";
import type { OutgoingNotification } from "../../src/jsonrpc/outgoing-message.js";
import type { ThreadId, TurnId } from "../../src/util/ids.js";

type AnyAsyncGen = AsyncGenerator<unknown, void, unknown>;
type MockQuery = (args: { prompt: string; options: unknown }) => AnyAsyncGen;

const makeArgs = (mockQuery: MockQuery) => {
  const threads = Threads.create();
  const thread = threads.start({
    cwd: "/tmp",
    permissionMode: "bypassPermissions",
  });
  const turnId = "turn_test" as TurnId;
  const sent: OutgoingNotification[] = [];
  const args: RunTurnArgs = {
    thread,
    turnId,
    input: [{ type: "text", text: "hi" }],
    abortController: new AbortController(),
    cli: {},
    threads,
    sendNotification: (n) => {
      sent.push(n);
    },
  };
  const session = Session.create({ query: mockQuery as never });
  return { args, sent, session };
};

const completedTurnError = (sent: OutgoingNotification[]) => {
  const ev = sent.find((n) => n.method === "turn/completed");
  if (!ev) throw new Error("turn/completed not sent");
  const turn = (ev.params as { turn: { error: unknown; status: string } }).turn;
  return { turn };
};

describe("Session.runTurn / usage limit", () => {
  it("flags codexErrorInfo:UsageLimitExceeded when rate_limit_event(rejected) precedes ResultError", async () => {
    const mockQuery: MockQuery = async function* () {
      yield {
        type: "system",
        subtype: "init",
        session_id: "s",
        uuid: "u",
      };
      yield {
        type: "rate_limit_event",
        rate_limit_info: { status: "rejected" },
        session_id: "s",
        uuid: "u",
      };
      yield {
        type: "result",
        subtype: "error_during_execution",
        session_id: "s",
        uuid: "u",
        duration_ms: 1,
        duration_api_ms: 1,
        is_error: true,
        num_turns: 1,
        stop_reason: null,
        total_cost_usd: 0,
        usage: {},
        modelUsage: {},
        permission_denials: [],
        errors: ["rate limited"],
      };
    };
    const { args, sent, session } = makeArgs(mockQuery);
    await session.runTurn(args);
    const { turn } = completedTurnError(sent);
    expect(turn.status).toBe("failed");
    expect(turn.error).toMatchObject({ codexErrorInfo: "UsageLimitExceeded" });
  });

  it("flags codexErrorInfo:UsageLimitExceeded when query() throws with 'rate limit' in message", async () => {
    const mockQuery: MockQuery = async function* () {
      yield {
        type: "system",
        subtype: "init",
        session_id: "s",
        uuid: "u",
      };
      throw new Error("hit rate limit on the API");
    };
    const { args, sent, session } = makeArgs(mockQuery);
    await session.runTurn(args);
    const { turn } = completedTurnError(sent);
    expect(turn.status).toBe("failed");
    expect(turn.error).toMatchObject({ codexErrorInfo: "UsageLimitExceeded" });
  });

  it("flags codexErrorInfo:UsageLimitExceeded when ResultError errors contains 'usage limit'", async () => {
    const mockQuery: MockQuery = async function* () {
      yield { type: "system", subtype: "init", session_id: "s", uuid: "u" };
      yield {
        type: "result",
        subtype: "error_during_execution",
        session_id: "s",
        uuid: "u",
        duration_ms: 1,
        duration_api_ms: 1,
        is_error: true,
        num_turns: 1,
        stop_reason: null,
        total_cost_usd: 0,
        usage: {},
        modelUsage: {},
        permission_denials: [],
        errors: ["account usage limit reached"],
      };
    };
    const { args, sent, session } = makeArgs(mockQuery);
    await session.runTurn(args);
    const { turn } = completedTurnError(sent);
    expect(turn.error).toMatchObject({ codexErrorInfo: "UsageLimitExceeded" });
  });

  it("does NOT add codexErrorInfo for generic ResultError without rate-limit hints", async () => {
    const mockQuery: MockQuery = async function* () {
      yield { type: "system", subtype: "init", session_id: "s", uuid: "u" };
      yield {
        type: "result",
        subtype: "error_during_execution",
        session_id: "s",
        uuid: "u",
        duration_ms: 1,
        duration_api_ms: 1,
        is_error: true,
        num_turns: 1,
        stop_reason: null,
        total_cost_usd: 0,
        usage: {},
        modelUsage: {},
        permission_denials: [],
        errors: ["something else broke"],
      };
    };
    const { args, sent, session } = makeArgs(mockQuery);
    await session.runTurn(args);
    const { turn } = completedTurnError(sent);
    expect(turn.status).toBe("failed");
    const err = turn.error as { message: string; codexErrorInfo?: string } | null;
    expect(err?.message).toContain("something else broke");
    expect(err?.codexErrorInfo).toBeUndefined();
  });

  it("ignores rate_limit_event with status:allowed (no UsageLimitExceeded)", async () => {
    const mockQuery: MockQuery = async function* () {
      yield { type: "system", subtype: "init", session_id: "s", uuid: "u" };
      yield {
        type: "rate_limit_event",
        rate_limit_info: { status: "allowed", utilization: 0.2 },
        session_id: "s",
        uuid: "u",
      };
      yield {
        type: "result",
        subtype: "success",
        session_id: "s",
        uuid: "u",
        duration_ms: 1,
        duration_api_ms: 1,
        is_error: false,
        num_turns: 1,
        stop_reason: null,
        total_cost_usd: 0,
        result: "ok",
        usage: {},
        modelUsage: {},
        permission_denials: [],
      };
    };
    const { args, sent, session } = makeArgs(mockQuery);
    await session.runTurn(args);
    const ev = sent.find((n) => n.method === "turn/completed");
    const turn = (ev?.params as { turn: { status: string; error: unknown } }).turn;
    expect(turn.status).toBe("completed");
    expect(turn.error).toBeNull();
  });
});
```

- [ ] **Step 2: Verify tests fail (import compile + assertion)**

```bash
pnpm test -- test/claude/session.test.ts
```

Expected: FAIL (`codexErrorInfo` 期待値が undefined → mismatch)。`RunTurnArgs` の型 export が無ければ session.ts 側を先に直す。

- [ ] **Step 3: Update session.ts to detect usage limit and emit codexErrorInfo**

`src/claude/session.ts` の修正:

1. `turnSnapshot` の `error` 型と関数本体を拡張:

```ts
type TurnError = Readonly<{ message: string; codexErrorInfo?: string }> | null;

const turnSnapshot = (
  args: RunTurnArgs,
  status: "inProgress" | "completed" | "failed" | "interrupted",
  error: TurnError,
) => ({
  id: args.turnId,
  status,
  items: [],
  error,
});
```

2. `runTurn` 関数本体の修正:

```ts
    runTurn: async (args) => {
      const { thread, threads, sendNotification, abortController } = args;

      sendNotification(
        OutgoingNotification.of("turn/started", {
          threadId: thread.id,
          turn: turnSnapshot(args, "inProgress", null),
        }),
      );

      const sdkOpts = toSdkOptions({
        thread: thread.options,
        resume: thread.sessionId,
        abortController,
      });

      let mapperState: MapperState = EventMapper.initialState();
      const mapCtx = { threadId: thread.id, turnId: args.turnId, cwd: thread.options.cwd };

      let status: "completed" | "failed" | "interrupted" = "completed";
      let error: TurnError = null;
      let usageLimitHit = false;

      const usageLimitPattern = /rate.?limit|usage.?limit|quota/i;
      const toUsageLimitError = (message: string): TurnError => ({
        message,
        codexErrorInfo: "UsageLimitExceeded",
      });

      try {
        for await (const raw of deps.query({ prompt: promptFrom(args.input), options: sdkOpts })) {
          for (const parsed of SdkMessage.parse(raw)) {
            if (parsed.kind === "SystemInit") {
              const sid = SessionId.parse(parsed.sessionId);
              if (sid.type === "Success") threads.setSessionId(thread.id, sid.value);
            }
            if (parsed.kind === "RateLimit" && parsed.status === "rejected") {
              usageLimitHit = true;
            }
            const r = EventMapper.map(parsed, mapperState, mapCtx);
            mapperState = r.state;
            for (const n of r.notifications) sendNotification(n);

            if (parsed.kind === "ResultError") {
              status = "failed";
              const message =
                parsed.errors.join("; ") || `result error: ${parsed.subtype}`;
              const matched = usageLimitHit || usageLimitPattern.test(message);
              error = matched ? toUsageLimitError(message) : { message };
            }
          }
        }
      } catch (err) {
        if (err instanceof AbortError || (err instanceof Error && err.name === "AbortError")) {
          status = "interrupted";
        } else {
          status = "failed";
          const message = err instanceof Error ? err.message : String(err);
          const matched = usageLimitHit || usageLimitPattern.test(message);
          error = matched ? toUsageLimitError(message) : { message };
        }
      } finally {
        const reason: CloseReason = status;
        const closed = EventMapper.closeAll(mapperState, mapCtx, reason);
        for (const n of closed.notifications) sendNotification(n);

        threads.clearCurrentTurn(thread.id);
        sendNotification(
          OutgoingNotification.of("turn/completed", {
            threadId: thread.id,
            turn: turnSnapshot(args, status, error),
          }),
        );
      }
    },
```

3. `RunTurnArgs` の export を確認 (既に `export type RunTurnArgs` で公開済み、追加変更不要)。

- [ ] **Step 4: Run session.test.ts to verify all pass**

```bash
pnpm test -- test/claude/session.test.ts
```

Expected: PASS (5 tests)

- [ ] **Step 5: Run the full test suite to verify no regressions**

```bash
pnpm test
```

Expected: 全 PASS (Phase 3 時点 131 件 + Phase 4 新規 stub 3 件 + 不明フィールド 1 件 + sdk-message 4 件 + session 5 件 ≒ 144 件)

- [ ] **Step 6: Commit**

```bash
git add apps/claude-app-server/src/claude/session.ts \
        apps/claude-app-server/test/claude/session.test.ts
git commit -m "feat(claude-app-server): map usage limit to turn.error.codexErrorInfo

rate_limit_event(rejected) 検出または ResultError/例外メッセージの
rate-limit パターンマッチで usage limit を判定し、turn/completed の
turn.error に { message, codexErrorInfo: 'UsageLimitExceeded' } を載せて
返す。Symphony はこのタグで失敗種別を識別できる。"
```

---

### Task 5: README 更新

Phase 4 で増えた挙動 (stub, レート制限) と Milestone 2 以降に延期した項目を反映する。

**Files:**
- Modify: `apps/claude-app-server/README.md`

- [ ] **Step 1: Update the diff table row for thread/tokenUsage/updated**

`README.md` の「Codex App Server 本家との既知の差異」テーブル内、`thread/tokenUsage/updated` 行 (45 行目あたり) の「理由」列を書き換える:

```
| `thread/tokenUsage/updated` | 都度発行 | 未発行 | Milestone 2 以降に延期 (本番投入直前に再判定) |
```

`outputDelta` 系の行 (`item/commandExecution/outputDelta`, `item/fileChange/outputDelta`) の理由列に「Milestone 2 以降の partial messages 対応で部分改善見込み」を追記しても良いが、現行記述で意味は通っているのでそのまま据え置きでも可。

- [ ] **Step 2: Add "未実装メソッド" section**

`README.md` の「Codex App Server 本家との既知の差異」セクションと「`fileChange.changes[*].diff` の取り扱い注意」セクションの間に挿入:

```markdown
## 未実装メソッド

Symphony が使わないメソッド (`account/*`, `fs/*`, `thread/fork`, `thread/list`, `turn/steer`, `command/exec`, `thread/resume` 等) は dispatcher のデフォルトで `-32601 Method not found` を返す。

スタブで擬似応答を返すメソッドは `src/handlers/stub.ts` の `registerStubs` に集約:

- `mcpServerStatus/list` → `{ "servers": [] }`

擬似応答を追加したいときは `registerStubs` 内で `registerStub(dispatcher, "<method>", <response>)` を 1 行足すだけ。挙動を分岐させたい場合は `dispatcher.registerRequest` で個別ハンドラを登録する。
```

- [ ] **Step 3: Add "レート制限" section**

「未実装メソッド」セクションの直後に挿入:

```markdown
## レート制限

Claude Agent SDK が `rate_limit_event` で `rate_limit_info.status: "rejected"` を流したターン、または例外 / `ResultError.errors[]` のメッセージに rate limit / usage limit / quota パターンが含まれるターンは、`turn/completed` 通知の `turn.error` に Codex 互換タグを載せて失敗扱いにする:

```json
{
  "method": "turn/completed",
  "params": {
    "turn": {
      "status": "failed",
      "error": {
        "message": "...",
        "codexErrorInfo": "UsageLimitExceeded"
      }
    }
  }
}
```

Symphony は `codexErrorInfo` を見てレート制限を識別する。リトライポリシーは Symphony 側で持つ。
```

- [ ] **Step 4: Commit**

```bash
git add apps/claude-app-server/README.md
git commit -m "docs(claude-app-server): document stub framework and usage limit handling

未実装メソッドの章で stub.ts による拡張可能フレームと mcpServerStatus/list の
スタブを説明。レート制限の章で turn/completed.turn.error.codexErrorInfo:
UsageLimitExceeded の仕様を明記。既知の差異テーブルから thread/tokenUsage/
updated を Milestone 2 以降に延期と更新。"
```

---

### Task 6: 最終検証 (build / lint / format / test)

すべての変更が揃った後、フル検証を回す。

- [ ] **Step 1: Run lint**

```bash
cd apps/claude-app-server
pnpm lint
```

Expected: no errors

- [ ] **Step 2: Run format check**

```bash
pnpm format:check
```

Expected: no diff

format:check で diff が出た場合は `pnpm format` を実行して書き直し、結果を commit に含める。

- [ ] **Step 3: Run full test suite**

```bash
pnpm test
```

Expected: 全 PASS (約 144 件)

- [ ] **Step 4: Run build**

```bash
pnpm build
```

Expected: tsc がエラーなく完了、`dist/bin.js` に実行権限が付く

- [ ] **Step 5: 手動シナリオの回帰確認 (任意、認証が通る環境でのみ)**

```bash
cat examples/manual_test.jsonl | node dist/bin.js 2>/tmp/cas-phase4-stderr.log
```

Expected: Phase 3 と同等のシーケンス (thread/started, turn/started, item/started, item/completed, turn/completed) が JSONL で stdout に流れること。stub などの新規挙動は Symphony 経由でないと自然には呼ばれないので、確認は次の Symphony Phase 2 / E2E に委ねて良い。

- [ ] **Step 6: Final commit (if format ran)**

format で差分が出ていれば:

```bash
git add -u apps/claude-app-server
git commit -m "chore(claude-app-server): apply oxfmt"
```

差分が無ければスキップ。

---

## 完了条件

- [ ] [`docs/superpowers/specs/2026-05-12-cas-phase4-design.md`](../specs/2026-05-12-cas-phase4-design.md) のスコープ表 (1)〜(5) すべてに対応 commit がブランチに積まれている
- [ ] `pnpm test` / `pnpm lint` / `pnpm format:check` / `pnpm build` がすべてクリア
- [ ] [`TODO.md`](../../../TODO.md) の Phase 4 セクションのチェックリストが全て完了する状態にできる (PR 説明で「[x] を付ける」予定旨を書く)
- [ ] PR を `feat/cas-phase4` → `main` で作成 (ユーザ承認後)
