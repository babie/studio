# `scripts/e2e.ts` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TODO.md 実行順 8 を完了する。`scripts/e2e.ts` (TypeScript) を作成し、`pnpm e2e` で「両アプリビルド → install → Linear issue リセット → Symphony 起動 → workspace 検証」を一発で実行できる受け入れテストにする。

**Architecture:** モノリシック TypeScript script。`@praha/byethrow` の `Result.pipe` / `Result.andThen` で全ステップを Result 型でチェイン、`valibot` で env / Linear API レスポンスを boundary validation。`tsdown` で `scripts/dist/e2e.js` に bundle し、`node` で実行。エラーは discriminated union + `assertNever` で網羅性担保。

**Tech Stack:** TypeScript / Node.js 20+ / `@praha/byethrow` / `valibot` / `tsdown` / Linear GraphQL API / `child_process.spawn`

**Spec:** [`docs/superpowers/specs/2026-05-14-e2e-script-design.md`](../specs/2026-05-14-e2e-script-design.md) (commit `20d0118`)

---

## File Structure

新規・更新するファイルと責務:

| パス | 種別 | 責務 |
|---|---|---|
| `scripts/e2e.ts` | 新規 | 受け入れテスト本体 (env parse / preflight / build / install / reset / spawn / verify / cleanup) |
| `.env.example` | 新規 | 環境変数テンプレート |
| `tsdown.config.ts` | 新規 | tsdown 設定 (entry, outDir, format) |
| `package.json` | 更新 | devDeps 追加 + `"e2e"` script を tsdown build → node 実行に |
| `.gitignore` | 更新 | `scripts/dist/` 追加 |
| `CLAUDE.md` (ルート) | 更新 | `scripts/e2e.sh` 参照と `mise exec` を最新化 |
| `README.md` (ルート) | 更新 | `build_all.sh` / `install_dev.sh` / `scripts/e2e.sh` / `mise exec` を最新化 |
| `TODO.md` | 更新 | 実行順 6 括弧書き / 実行順 8 チェック / Milestone 1 完了条件 |
| `docs/e2e_testing.md` | 更新 | bash 版サンプル → TS 版、`.env.example` セクション追加 |
| `docs/adr/0001-adopt-monorepo.md` | 更新 | `scripts/e2e.sh` → `scripts/e2e.ts` |
| `apps/claude-app-server/CLAUDE.md` | 更新 | `scripts/e2e.sh` 参照を更新 |
| `apps/symphony/CLAUDE.md` | 更新 | `mise exec` 削除 + 完了条件チェック |

---

## Task 1: ブートストラップ — devDeps / `.env.example` / `.gitignore` / tsdown 設定

**Files:**
- Modify: `/workspace/package.json`
- Create: `/workspace/.env.example`
- Modify: `/workspace/.gitignore`
- Create: `/workspace/tsdown.config.ts`

- [ ] **Step 1: devDeps を追加**

Run:
```bash
cd /workspace && pnpm add -wD @praha/byethrow valibot tsdown @types/node
```

Expected output: `+ @praha/byethrow ...`, `+ valibot ...`, `+ tsdown ...`, `+ @types/node ...`、ルート `package.json` の `devDependencies` に 4 つが追加される。`-w` は workspace root を意味する。

- [ ] **Step 2: `package.json` の `"e2e"` script を書き換え**

`/workspace/package.json` の `"scripts"` を以下のように:

```json
{
  "scripts": {
    "build": "pnpm build:claude-app-server && pnpm build:symphony",
    "build:claude-app-server": "pnpm --filter claude-app-server build",
    "build:symphony": "cd apps/symphony && mix build",
    "test": "pnpm test:claude-app-server && pnpm test:symphony",
    "test:claude-app-server": "pnpm --filter claude-app-server test",
    "test:symphony": "cd apps/symphony && mix test",
    "dev:install": "cd apps/claude-app-server && pnpm link --global",
    "dev:uninstall": "pnpm uninstall -g claude-app-server || true",
    "e2e:build": "tsdown",
    "e2e": "pnpm e2e:build && node scripts/dist/e2e.js"
  }
}
```

`"e2e"` だけ書き換える (他の script は触らない)。

- [ ] **Step 3: `tsdown.config.ts` を作成**

`/workspace/tsdown.config.ts`:

```ts
import { defineConfig } from 'tsdown';

export default defineConfig({
  entry: ['scripts/e2e.ts'],
  outDir: 'scripts/dist',
  format: 'esm',
  platform: 'node',
  target: 'node20',
  dts: false,
  clean: true,
});
```

- [ ] **Step 4: `.env.example` を作成**

`/workspace/.env.example`:

```bash
# Linear Personal API key (Settings → Security & access → Personal API keys)
# Symphony と scripts/e2e.ts の両方が読む
LINEAR_API_KEY=

# scripts/e2e.ts が処理する Linear issue の identifier (例: CYFY-5)
# 必須。state は実行時にスクリプトが Todo (or E2E_LINEAR_RESET_STATE) に戻す
E2E_LINEAR_ISSUE_KEY=

# 以下は省略可 (コメントに記載のデフォルトが使われる)

# リセット先 state 名 (default: Todo)
# E2E_LINEAR_RESET_STATE=Todo

# Symphony に渡す workflow ファイル (default: apps/symphony/examples/workflow.claude.md)
# E2E_WORKFLOW_PATH=apps/symphony/examples/workflow.claude.md

# 検証用 workspace の親ディレクトリ (default: /tmp/concert-e2e/workspaces)
# E2E_WORKSPACE_ROOT=/tmp/concert-e2e/workspaces

# Symphony 起動の上限秒数 (default: 300)
# E2E_TIMEOUT_SECONDS=300
```

- [ ] **Step 5: `.gitignore` に `scripts/dist/` を追加**

`/workspace/.gitignore` の適当な位置 (`# DevContainer / Nix` ブロック手前) に挿入:

```diff
 # Logs
 *.log

+# Build outputs
+scripts/dist/
+
 # DevContainer / Nix
 .env
 .env.example
```

待って `.env.example` は ignored してはいけない (commit する)。`.env.example` の行が既に gitignore に居るなら**消す**。確認:

Run: `grep -n "env.example" /workspace/.gitignore`  
もしヒットしたら該当行を削除。`.env` のみ ignore。

- [ ] **Step 6: tsdown が動くか確認 (空 e2e.ts で smoke build)**

`/workspace/scripts/e2e.ts` を仮で作る:

```ts
console.log("e2e.ts placeholder");
```

Run: `cd /workspace && pnpm e2e:build`  
Expected: `scripts/dist/e2e.js` が生成される、エラーなし。

Run: `node /workspace/scripts/dist/e2e.js`  
Expected output: `e2e.ts placeholder`

- [ ] **Step 7: commit**

```bash
cd /workspace
git add package.json pnpm-lock.yaml tsdown.config.ts .env.example .gitignore scripts/e2e.ts
git commit -m "$(cat <<'EOF'
chore(repo): bootstrap scripts/e2e.ts toolchain

- Add devDeps: @praha/byethrow, valibot, tsdown, @types/node
- Add tsdown.config.ts targeting node20 ESM
- Rework "e2e" script to tsdown build + node run
- Add .env.example as template for required E2E env vars
- Ignore scripts/dist/ build output

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: env schema + E2EError 型 + main 骨格 + exit code マッピング

**Files:**
- Modify: `/workspace/scripts/e2e.ts`

このタスクで、env を valibot で parse して `ConfigError` 経由で exit 2、env が揃ったら exit 0 を返す最小骨格を作る。本格的なステップは後続タスクで追加。

- [ ] **Step 1: `scripts/e2e.ts` 全置換** — env / error / main 骨格

`/workspace/scripts/e2e.ts`:

```ts
#!/usr/bin/env node
import { Result } from '@praha/byethrow';
import * as v from 'valibot';

// =====================================================================
// Environment
// =====================================================================

const EnvSchema = v.object({
  LINEAR_API_KEY: v.pipe(v.string(), v.minLength(1, 'LINEAR_API_KEY is required')),
  E2E_LINEAR_ISSUE_KEY: v.pipe(v.string(), v.minLength(1, 'E2E_LINEAR_ISSUE_KEY is required')),
  E2E_LINEAR_RESET_STATE: v.optional(v.string(), 'Todo'),
  E2E_WORKFLOW_PATH: v.optional(v.string(), 'apps/symphony/examples/workflow.claude.md'),
  E2E_WORKSPACE_ROOT: v.optional(v.string(), '/tmp/concert-e2e/workspaces'),
  E2E_TIMEOUT_SECONDS: v.optional(
    v.pipe(v.string(), v.transform(Number), v.number(), v.minValue(1)),
    '300',
  ),
});

type Env = v.InferOutput<typeof EnvSchema>;

// =====================================================================
// Error type
// =====================================================================

type E2EError =
  | { kind: 'ConfigError'; issues: ReadonlyArray<string> }
  | { kind: 'ToolMissing'; tools: ReadonlyArray<string> }
  | { kind: 'BuildFailed'; phase: 'claude-app-server' | 'symphony'; code: number }
  | { kind: 'InstallFailed'; code: number }
  | { kind: 'LinearHttpError'; status: number; body: string }
  | { kind: 'LinearGraphQLError'; errors: ReadonlyArray<{ message: string }> }
  | { kind: 'LinearResponseInvalid'; issues: ReadonlyArray<string> }
  | { kind: 'StateNotFound'; want: string; available: ReadonlyArray<string> }
  | { kind: 'SymphonySpawnFailed'; cause: string }
  | { kind: 'SymphonyTimedOut'; logPath: string }
  | {
      kind: 'VerifyFailed';
      check: 'fileExists' | 'nodeOutput' | 'gitCommitted';
      detail: string;
      workspacePath: string;
    }
  | { kind: 'Interrupted' };

function assertNever(x: never): never {
  throw new Error(`unhandled error variant: ${JSON.stringify(x)}`);
}

// =====================================================================
// Env parsing
// =====================================================================

function parseEnv(): Result.Result<Env, E2EError> {
  const result = v.safeParse(EnvSchema, process.env);
  if (result.success) {
    return Result.succeed(result.output);
  }
  const issues = result.issues.map(
    (i) => `${i.path?.map((p) => p.key).join('.') ?? '<root>'}: ${i.message}`,
  );
  return Result.fail({ kind: 'ConfigError', issues });
}

// =====================================================================
// Error → exit code + message
// =====================================================================

function reportError(error: E2EError): number {
  switch (error.kind) {
    case 'ConfigError':
      console.error('FAIL: invalid env');
      for (const issue of error.issues) console.error(`  - ${issue}`);
      return 2;
    case 'ToolMissing':
      console.error(`FAIL: missing required tools: ${error.tools.join(', ')}`);
      return 2;
    case 'BuildFailed':
      console.error(`FAIL: build of ${error.phase} exited with code ${error.code}`);
      return 1;
    case 'InstallFailed':
      console.error(`FAIL: pnpm dev:install exited with code ${error.code}`);
      return 1;
    case 'LinearHttpError':
      console.error(`FAIL: Linear HTTP ${error.status}: ${error.body}`);
      return 1;
    case 'LinearGraphQLError':
      console.error('FAIL: Linear GraphQL errors:');
      for (const e of error.errors) console.error(`  - ${e.message}`);
      return 1;
    case 'LinearResponseInvalid':
      console.error('FAIL: Linear response did not match schema:');
      for (const i of error.issues) console.error(`  - ${i}`);
      return 1;
    case 'StateNotFound':
      console.error(`FAIL: state "${error.want}" not found in team`);
      console.error(`  available: ${error.available.join(', ')}`);
      return 1;
    case 'SymphonySpawnFailed':
      console.error(`FAIL: symphony spawn failed: ${error.cause}`);
      return 1;
    case 'SymphonyTimedOut':
      console.error(`FAIL: symphony timed out; see ${error.logPath}`);
      return 1;
    case 'VerifyFailed':
      console.error(`FAIL: verification (${error.check}): ${error.detail}`);
      console.error(`  workspace: ${error.workspacePath}`);
      return 1;
    case 'Interrupted':
      console.error('FAIL: interrupted');
      return 130;
    default:
      return assertNever(error);
  }
}

// =====================================================================
// Main
// =====================================================================

async function main(): Promise<number> {
  const envResult = parseEnv();
  if (Result.isFailure(envResult)) {
    return reportError(envResult.error);
  }
  // const env = envResult.value;
  // TODO: subsequent tasks add preflight / build / install / reset / runSymphony / verify here

  console.log('PASS (scaffold)');
  return 0;
}

main().then(
  (code) => process.exit(code),
  (err) => {
    console.error('FAIL: unexpected exception:', err);
    process.exit(1);
  },
);
```

- [ ] **Step 2: build を実行**

Run: `cd /workspace && pnpm e2e:build`  
Expected: エラーなし、`scripts/dist/e2e.js` が更新される。tsc 型エラーがあれば直す (例: `valibot` の `v.InferOutput` 型が認識されない場合 `@types/node` を確認)。

- [ ] **Step 3: 必須 env なしで run → ConfigError, exit 2**

Run:
```bash
cd /workspace
unset LINEAR_API_KEY E2E_LINEAR_ISSUE_KEY
pnpm e2e
echo "exit=$?"
```

Expected:
```
FAIL: invalid env
  - LINEAR_API_KEY: LINEAR_API_KEY is required
  - E2E_LINEAR_ISSUE_KEY: E2E_LINEAR_ISSUE_KEY is required
exit=2
```

- [ ] **Step 4: env を埋めて run → PASS (scaffold), exit 0**

Run:
```bash
cd /workspace
LINEAR_API_KEY=x E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```

Expected:
```
PASS (scaffold)
exit=0
```

- [ ] **Step 5: commit**

```bash
cd /workspace
git add scripts/e2e.ts
git commit -m "$(cat <<'EOF'
feat(scripts): scaffold e2e.ts with env schema and error union

Establish the kamae-flavored skeleton:
- valibot EnvSchema for required + optional env
- E2EError discriminated union covering every failure mode
- reportError() with assertNever for exhaustiveness
- main() that parses env and exits 0/2 with no real work yet

Subsequent tasks fill in preflight, build, install, Linear reset,
Symphony spawn, and workspace verification.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `preflight()` — ANTHROPIC_API_KEY unset + 外部ツール存在チェック

**Files:**
- Modify: `/workspace/scripts/e2e.ts`

- [ ] **Step 1: 必要な import を追加**

`/workspace/scripts/e2e.ts` 冒頭の import に以下を追加:

```ts
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';

const execFileP = promisify(execFile);
```

- [ ] **Step 2: `preflight()` 実装** (env parse の関数の下に追加)

```ts
// =====================================================================
// Preflight
// =====================================================================

const REQUIRED_TOOLS = ['pnpm', 'mix', 'node', 'git'] as const;

async function checkTool(name: string): Promise<boolean> {
  try {
    await execFileP('which', [name]);
    return true;
  } catch {
    return false;
  }
}

async function preflight(): Promise<Result.Result<void, E2EError>> {
  if (process.env.ANTHROPIC_API_KEY) {
    console.warn(
      'preflight: unsetting ANTHROPIC_API_KEY to force Pro/Max subscription auth',
    );
    delete process.env.ANTHROPIC_API_KEY;
  }
  const missing: string[] = [];
  for (const tool of REQUIRED_TOOLS) {
    if (!(await checkTool(tool))) missing.push(tool);
  }
  if (missing.length > 0) {
    return Result.fail({ kind: 'ToolMissing', tools: missing });
  }
  return Result.succeed(undefined);
}
```

- [ ] **Step 3: `main()` に preflight を組み込む**

`main()` の `// TODO: subsequent tasks...` コメントを以下に置き換え:

```ts
  const env = envResult.value;

  console.log('→ preflight');
  const preflightResult = await preflight();
  if (Result.isFailure(preflightResult)) {
    return reportError(preflightResult.error);
  }

  // TODO: build / install / reset / runSymphony / verify
  void env;
  console.log('PASS (scaffold)');
  return 0;
```

- [ ] **Step 4: build + run で検証**

```bash
cd /workspace
LINEAR_API_KEY=x E2E_LINEAR_ISSUE_KEY=y pnpm e2e
echo "exit=$?"
```

Expected:
```
→ preflight
PASS (scaffold)
exit=0
```

(devShell 内で pnpm/mix/node/git は揃ってる前提)

ANTHROPIC_API_KEY ありで検証:
```bash
cd /workspace
ANTHROPIC_API_KEY=fake LINEAR_API_KEY=x E2E_LINEAR_ISSUE_KEY=y pnpm e2e
```

Expected: `preflight: unsetting ANTHROPIC_API_KEY ...` の warn が出る。

- [ ] **Step 5: commit**

```bash
cd /workspace
git add scripts/e2e.ts
git commit -m "$(cat <<'EOF'
feat(scripts): add preflight to e2e.ts

Unset ANTHROPIC_API_KEY (force Pro/Max auth) and verify required CLI
tools (pnpm/mix/node/git) are on PATH; report missing tools via
ToolMissing.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `cleanWorkspace()` + `build()` + `install()`

**Files:**
- Modify: `/workspace/scripts/e2e.ts`

- [ ] **Step 1: import を追加**

```ts
import { rm } from 'node:fs/promises';
import * as path from 'node:path';
import { spawn } from 'node:child_process';
```

- [ ] **Step 2: 子プロセスヘルパ + cleanWorkspace + build + install を追加**

`preflight()` の下に挿入:

```ts
// =====================================================================
// Child-process helper (inherit stdio, return exit code)
// =====================================================================

function runChild(
  command: string,
  args: ReadonlyArray<string>,
  options: { cwd?: string } = {},
): Promise<number> {
  return new Promise((resolve, reject) => {
    const child = spawn(command, [...args], {
      cwd: options.cwd ?? process.cwd(),
      env: process.env,
      stdio: 'inherit',
    });
    child.once('error', reject);
    child.once('exit', (code) => resolve(code ?? -1));
  });
}

// =====================================================================
// Steps: clean / build / install
// =====================================================================

const REPO_ROOT = path.resolve(import.meta.dirname, '..');

async function cleanWorkspace(env: Env): Promise<Result.Result<void, E2EError>> {
  const target = path.join(env.E2E_WORKSPACE_ROOT, env.E2E_LINEAR_ISSUE_KEY);
  await rm(target, { recursive: true, force: true });
  return Result.succeed(undefined);
}

async function buildClaudeAppServer(): Promise<Result.Result<void, E2EError>> {
  const code = await runChild('pnpm', ['build:claude-app-server'], { cwd: REPO_ROOT });
  if (code !== 0) {
    return Result.fail({ kind: 'BuildFailed', phase: 'claude-app-server', code });
  }
  return Result.succeed(undefined);
}

async function buildSymphony(): Promise<Result.Result<void, E2EError>> {
  const code = await runChild('mix', ['compile'], {
    cwd: path.join(REPO_ROOT, 'apps/symphony'),
  });
  if (code !== 0) {
    return Result.fail({ kind: 'BuildFailed', phase: 'symphony', code });
  }
  return Result.succeed(undefined);
}

async function installClaudeAppServer(): Promise<Result.Result<void, E2EError>> {
  const code = await runChild('pnpm', ['dev:install'], { cwd: REPO_ROOT });
  if (code !== 0) {
    return Result.fail({ kind: 'InstallFailed', code });
  }
  return Result.succeed(undefined);
}
```

- [ ] **Step 3: `main()` を更新してチェイン**

`main()` を以下に置き換え:

```ts
async function main(): Promise<number> {
  const envResult = parseEnv();
  if (Result.isFailure(envResult)) return reportError(envResult.error);
  const env = envResult.value;

  console.log('→ preflight');
  const pre = await preflight();
  if (Result.isFailure(pre)) return reportError(pre.error);

  console.log(`→ clean workspace ${path.join(env.E2E_WORKSPACE_ROOT, env.E2E_LINEAR_ISSUE_KEY)}`);
  const clean = await cleanWorkspace(env);
  if (Result.isFailure(clean)) return reportError(clean.error);

  console.log('→ build claude-app-server');
  const b1 = await buildClaudeAppServer();
  if (Result.isFailure(b1)) return reportError(b1.error);

  console.log('→ build symphony');
  const b2 = await buildSymphony();
  if (Result.isFailure(b2)) return reportError(b2.error);

  console.log('→ install claude-app-server (pnpm dev:install)');
  const inst = await installClaudeAppServer();
  if (Result.isFailure(inst)) return reportError(inst.error);

  // TODO: reset / runSymphony / verify
  console.log('PASS (scaffold)');
  return 0;
}
```

- [ ] **Step 4: 実行して build/install まで通ることを確認**

```bash
cd /workspace
LINEAR_API_KEY=x E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```

Expected: claude-app-server / symphony のビルドログが流れて、`pnpm dev:install` まで通って `PASS (scaffold) exit=0`。失敗するなら直す。

- [ ] **Step 5: commit**

```bash
cd /workspace
git add scripts/e2e.ts
git commit -m "$(cat <<'EOF'
feat(scripts): add clean/build/install steps to e2e.ts

cleanWorkspace removes the per-issue workspace dir before each run.
buildClaudeAppServer / buildSymphony run the corresponding pnpm scripts,
installClaudeAppServer runs pnpm dev:install. All non-zero exits map to
BuildFailed / InstallFailed.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Linear GraphQL — `linearGraphql()` + `fetchIssue()` + `resetIssueToTodo()`

**Files:**
- Modify: `/workspace/scripts/e2e.ts`

- [ ] **Step 1: Linear レスポンス schema を追加**

`E2EError` 型の下に追加:

```ts
// =====================================================================
// Linear GraphQL schemas
// =====================================================================

const StateNodeSchema = v.object({ id: v.string(), name: v.string() });

const IssueResponseSchema = v.object({
  data: v.object({
    issue: v.object({
      id: v.string(),
      identifier: v.string(),
      team: v.object({
        states: v.object({ nodes: v.array(StateNodeSchema) }),
      }),
    }),
  }),
});

const ResetResponseSchema = v.object({
  data: v.object({
    issueUpdate: v.object({
      success: v.boolean(),
      issue: v.object({ state: v.object({ name: v.string() }) }),
    }),
  }),
});

const StatusResponseSchema = v.object({
  data: v.object({
    issue: v.object({ state: v.object({ name: v.string() }) }),
  }),
});

type IssueWithStates = v.InferOutput<typeof IssueResponseSchema>['data']['issue'];
```

- [ ] **Step 2: `linearGraphql()` ヘルパを追加**

`installClaudeAppServer()` の下に挿入:

```ts
// =====================================================================
// Linear GraphQL
// =====================================================================

const LINEAR_ENDPOINT = 'https://api.linear.app/graphql';

async function linearGraphql<TSchema extends v.GenericSchema>(
  apiKey: string,
  query: string,
  variables: Record<string, unknown>,
  schema: TSchema,
): Promise<Result.Result<v.InferOutput<TSchema>, E2EError>> {
  let res: Response;
  try {
    res = await fetch(LINEAR_ENDPOINT, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: apiKey,
      },
      body: JSON.stringify({ query, variables }),
    });
  } catch (err) {
    return Result.fail({
      kind: 'LinearHttpError',
      status: 0,
      body: err instanceof Error ? err.message : String(err),
    });
  }
  if (!res.ok) {
    const body = await res.text().catch(() => '<unreadable>');
    return Result.fail({ kind: 'LinearHttpError', status: res.status, body });
  }
  const json = (await res.json().catch(() => null)) as unknown;
  if (json && typeof json === 'object' && 'errors' in json) {
    const errors = (json as { errors?: unknown }).errors;
    if (Array.isArray(errors) && errors.length > 0) {
      const messages = errors.map((e) =>
        e && typeof e === 'object' && 'message' in e ? { message: String((e as { message: unknown }).message) } : { message: JSON.stringify(e) },
      );
      return Result.fail({ kind: 'LinearGraphQLError', errors: messages });
    }
  }
  const parsed = v.safeParse(schema, json);
  if (!parsed.success) {
    const issues = parsed.issues.map(
      (i) => `${i.path?.map((p) => p.key).join('.') ?? '<root>'}: ${i.message}`,
    );
    return Result.fail({ kind: 'LinearResponseInvalid', issues });
  }
  return Result.succeed(parsed.output);
}
```

- [ ] **Step 3: `fetchIssue()` と `resetIssueToTodo()` を追加**

```ts
const FETCH_ISSUE_QUERY = `
  query GetIssue($id: String!) {
    issue(id: $id) {
      id
      identifier
      team {
        states {
          nodes { id name }
        }
      }
    }
  }
`;

const RESET_MUTATION = `
  mutation Reset($id: String!, $stateId: String!) {
    issueUpdate(id: $id, input: { stateId: $stateId }) {
      success
      issue { state { name } }
    }
  }
`;

async function fetchIssue(
  apiKey: string,
  issueKey: string,
): Promise<Result.Result<IssueWithStates, E2EError>> {
  const r = await linearGraphql(
    apiKey,
    FETCH_ISSUE_QUERY,
    { id: issueKey },
    IssueResponseSchema,
  );
  if (Result.isFailure(r)) return r;
  return Result.succeed(r.value.data.issue);
}

async function resetIssueToTodo(
  env: Env,
  issue: IssueWithStates,
): Promise<Result.Result<void, E2EError>> {
  const want = env.E2E_LINEAR_RESET_STATE;
  const stateNode = issue.team.states.nodes.find((s) => s.name === want);
  if (!stateNode) {
    return Result.fail({
      kind: 'StateNotFound',
      want,
      available: issue.team.states.nodes.map((s) => s.name),
    });
  }
  const r = await linearGraphql(
    env.LINEAR_API_KEY,
    RESET_MUTATION,
    { id: issue.id, stateId: stateNode.id },
    ResetResponseSchema,
  );
  if (Result.isFailure(r)) return r;
  if (!r.value.data.issueUpdate.success) {
    return Result.fail({
      kind: 'LinearGraphQLError',
      errors: [{ message: 'issueUpdate returned success=false' }],
    });
  }
  return Result.succeed(undefined);
}
```

- [ ] **Step 4: `main()` に reset ステップを組み込む**

`installClaudeAppServer` の後、`// TODO: reset / runSymphony / verify` を以下に置換:

```ts
  console.log(`→ fetch Linear issue ${env.E2E_LINEAR_ISSUE_KEY}`);
  const issueResult = await fetchIssue(env.LINEAR_API_KEY, env.E2E_LINEAR_ISSUE_KEY);
  if (Result.isFailure(issueResult)) return reportError(issueResult.error);
  const issue = issueResult.value;

  console.log(`→ reset issue ${issue.identifier} to "${env.E2E_LINEAR_RESET_STATE}"`);
  const resetResult = await resetIssueToTodo(env, issue);
  if (Result.isFailure(resetResult)) return reportError(resetResult.error);

  // TODO: runSymphony / verify
  console.log('PASS (scaffold)');
  return 0;
```

- [ ] **Step 5: 実 Linear に対して run 検証**

```bash
cd /workspace
# direnv が .env から LINEAR_API_KEY を読む環境で:
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```

Expected:
```
→ preflight
→ clean workspace ...
→ build claude-app-server
→ build symphony
→ install claude-app-server (pnpm dev:install)
→ fetch Linear issue CYFY-5
→ reset issue CYFY-5 to "Todo"
PASS (scaffold)
exit=0
```

Linear UI で CYFY-5 が Todo 状態にあることを確認。

ネガティブケース:
```bash
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=BOGUS-9999 pnpm e2e
```
Expected: `LinearGraphQLError` か `LinearResponseInvalid` のいずれかで exit 1。

- [ ] **Step 6: commit**

```bash
cd /workspace
git add scripts/e2e.ts
git commit -m "$(cat <<'EOF'
feat(scripts): add Linear GraphQL fetch + reset to e2e.ts

linearGraphql() wraps fetch + valibot schema validation, surfacing
LinearHttpError / LinearGraphQLError / LinearResponseInvalid for each
failure layer. fetchIssue resolves the issue UUID and its team's
workflow states; resetIssueToTodo finds the target state by name and
issues an issueUpdate mutation.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `runSymphony()` — spawn + log redirect + termination

**Files:**
- Modify: `/workspace/scripts/e2e.ts`

このタスクでは spawn して短時間で kill する (まだ polling は無し)。Task 7 で polling + timeout を合わせる。

- [ ] **Step 1: 必要な import を追加**

```ts
import { createWriteStream } from 'node:fs';
import { setTimeout as delay } from 'node:timers/promises';
import type { ChildProcess } from 'node:child_process';
```

- [ ] **Step 2: Symphony spawn + 制御ロジック (Task 7 で完成、ここは骨組み)**

`resetIssueToTodo()` の下に追加:

```ts
// =====================================================================
// Symphony spawn
// =====================================================================

const SYMPHONY_LOG_PATH = path.join(REPO_ROOT, 'scripts/e2e.symphony.log');

type SymphonyOutcome =
  | { kind: 'terminal' }
  | { kind: 'timeout' }
  | { kind: 'interrupted' }
  | { kind: 'exited'; code: number };

async function killGracefully(child: ChildProcess): Promise<void> {
  if (child.exitCode !== null || child.killed) return;
  child.kill('SIGTERM');
  for (let i = 0; i < 50; i++) {
    if (child.exitCode !== null) return;
    await delay(100);
  }
  if (child.exitCode === null) child.kill('SIGKILL');
}

function spawnSymphony(env: Env): Result.Result<ChildProcess, E2EError> {
  const workflowAbs = path.resolve(REPO_ROOT, env.E2E_WORKFLOW_PATH);
  const symphonyDir = path.join(REPO_ROOT, 'apps/symphony');
  const logStream = createWriteStream(SYMPHONY_LOG_PATH, { flags: 'a' });
  logStream.write(`\n=== ${new Date().toISOString()} ${env.E2E_LINEAR_ISSUE_KEY} ===\n`);
  let child: ChildProcess;
  try {
    child = spawn(
      './bin/symphony',
      [
        '--i-understand-that-this-will-be-running-without-the-usual-guardrails',
        workflowAbs,
      ],
      { cwd: symphonyDir, env: process.env, stdio: ['ignore', 'pipe', 'pipe'] },
    );
  } catch (err) {
    return Result.fail({
      kind: 'SymphonySpawnFailed',
      cause: err instanceof Error ? err.message : String(err),
    });
  }
  child.stdout?.pipe(logStream, { end: false });
  child.stderr?.pipe(logStream, { end: false });
  child.once('exit', () => logStream.end());
  return Result.succeed(child);
}
```

- [ ] **Step 3: 短時間 spawn → kill のテストルートを `main()` に追加 (Task 7 で本実装に置換)**

`main()` の `// TODO: runSymphony / verify` を一旦以下に:

```ts
  console.log('→ spawn symphony (smoke test 5s)');
  const spawnResult = spawnSymphony(env);
  if (Result.isFailure(spawnResult)) return reportError(spawnResult.error);
  const child = spawnResult.value;
  await delay(5000);
  console.log('→ kill symphony');
  await killGracefully(child);

  // TODO: replace smoke with polling + timeout + verify
  console.log('PASS (scaffold)');
  return 0;
```

- [ ] **Step 4: 実 Linear で 5 秒 spawn → kill が動くか確認**

```bash
cd /workspace
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```

Expected: 5秒待った後 `→ kill symphony` 表示 → exit 0。

Symphony プロセスが残っていないことを確認:
```bash
pgrep -af nil_escript || echo "no symphony processes"
pgrep -af claude-app-server || echo "no claude-app-server processes"
```

ログ確認:
```bash
tail -n 20 /workspace/scripts/e2e.symphony.log
```
Symphony の起動メッセージや TUI が記録されていること。

- [ ] **Step 5: commit**

```bash
cd /workspace
git add scripts/e2e.ts
git commit -m "$(cat <<'EOF'
feat(scripts): spawn symphony with log redirection in e2e.ts

spawnSymphony launches ./bin/symphony with the required --i-understand
flag, piping stdout+stderr to scripts/e2e.symphony.log (appended).
killGracefully sends SIGTERM, waits up to 5s, then SIGKILL.

Smoke harness in main runs the child for 5 seconds and exits, to be
replaced with polling + timeout in the next task.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Linear polling + timeout + SIGINT handling (`runSymphonyUntilTerminal`)

**Files:**
- Modify: `/workspace/scripts/e2e.ts`

- [ ] **Step 1: `pollIssueStatus()` を追加**

`spawnSymphony` の下に:

```ts
const STATUS_QUERY = `
  query GetStatus($id: String!) {
    issue(id: $id) { state { name } }
  }
`;

const TERMINAL_STATES: ReadonlySet<string> = new Set([
  'Done',
  'Closed',
  'Cancelled',
  'Canceled',
  'Duplicate',
]);

async function pollIssueStatus(
  env: Env,
): Promise<Result.Result<{ state: string; terminal: boolean }, E2EError>> {
  const r = await linearGraphql(
    env.LINEAR_API_KEY,
    STATUS_QUERY,
    { id: env.E2E_LINEAR_ISSUE_KEY },
    StatusResponseSchema,
  );
  if (Result.isFailure(r)) return r;
  const name = r.value.data.issue.state.name;
  return Result.succeed({ state: name, terminal: TERMINAL_STATES.has(name) });
}
```

- [ ] **Step 2: `runSymphonyUntilTerminal()` を追加**

```ts
async function runSymphonyUntilTerminal(
  env: Env,
): Promise<Result.Result<void, E2EError>> {
  const spawnResult = spawnSymphony(env);
  if (Result.isFailure(spawnResult)) return spawnResult;
  const child = spawnResult.value;

  let interrupted = false;
  const onSigint = () => {
    interrupted = true;
  };
  process.once('SIGINT', onSigint);

  const deadline = Date.now() + env.E2E_TIMEOUT_SECONDS * 1000;
  let outcome: SymphonyOutcome | null = null;

  try {
    // First poll a bit later so Symphony has time to claim the issue
    await delay(10_000);
    while (Date.now() < deadline) {
      if (interrupted) {
        outcome = { kind: 'interrupted' };
        break;
      }
      if (child.exitCode !== null) {
        outcome = { kind: 'exited', code: child.exitCode };
        break;
      }
      const status = await pollIssueStatus(env);
      if (Result.isFailure(status)) {
        // Transient Linear error: keep going, but record latest err in case we end up timing out
        console.warn(`  (poll error: ${status.error.kind}, retrying)`);
      } else if (status.value.terminal) {
        console.log(`  issue reached "${status.value.state}", exiting symphony`);
        outcome = { kind: 'terminal' };
        break;
      } else {
        console.log(`  status="${status.value.state}", waiting...`);
      }
      await delay(10_000);
    }
    if (!outcome) outcome = { kind: 'timeout' };
  } finally {
    process.off('SIGINT', onSigint);
    await killGracefully(child);
  }

  switch (outcome.kind) {
    case 'terminal':
      return Result.succeed(undefined);
    case 'timeout':
      return Result.fail({ kind: 'SymphonyTimedOut', logPath: SYMPHONY_LOG_PATH });
    case 'interrupted':
      return Result.fail({ kind: 'Interrupted' });
    case 'exited':
      return Result.fail({
        kind: 'SymphonySpawnFailed',
        cause: `symphony exited unexpectedly with code ${outcome.code}`,
      });
    default:
      return assertNever(outcome);
  }
}
```

- [ ] **Step 3: `main()` の smoke ハーネスを差し替え**

`spawnSymphony` を直接呼ぶ部分を以下に置換:

```ts
  console.log('→ run symphony until issue reaches terminal state');
  const sym = await runSymphonyUntilTerminal(env);
  if (Result.isFailure(sym)) return reportError(sym.error);

  // TODO: verify
  console.log('PASS (scaffold)');
  return 0;
```

- [ ] **Step 4: 実 Linear で end-to-end (verify 抜き) 動作確認**

事前準備: CYFY-5 の description が `tmp/hello.js` 仕様になっていることを Linear UI で確認 (docs/e2e_testing.md 5.1 節参照)。

```bash
cd /workspace
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```

Expected: 2〜5 分で `issue reached "Done", exiting symphony` → `PASS (scaffold)` → `exit=0`。  
失敗時は `scripts/e2e.symphony.log` で Symphony 出力を確認。

タイムアウト経路の確認:
```bash
E2E_TIMEOUT_SECONDS=5 LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```
Expected: `SymphonyTimedOut`, exit 1。

- [ ] **Step 5: commit**

```bash
cd /workspace
git add scripts/e2e.ts
git commit -m "$(cat <<'EOF'
feat(scripts): poll Linear until terminal state and kill symphony

runSymphonyUntilTerminal owns the lifecycle: spawn, poll the issue
status every 10 s, exit cleanly when Done/Closed/Cancelled/Duplicate is
observed, or surface SymphonyTimedOut / Interrupted / SymphonySpawnFailed
according to which guard tripped first. Always kills the child in
finally to avoid stranded subprocesses.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: `verifyWorkspace()` — file exists + node 実行 + git log

**Files:**
- Modify: `/workspace/scripts/e2e.ts`

- [ ] **Step 1: import を追加**

```ts
import { existsSync } from 'node:fs';
```

- [ ] **Step 2: verifyWorkspace を追加**

`runSymphonyUntilTerminal` の下に:

```ts
// =====================================================================
// Workspace verification
// =====================================================================

async function verifyWorkspace(env: Env): Promise<Result.Result<void, E2EError>> {
  const workspacePath = path.join(env.E2E_WORKSPACE_ROOT, env.E2E_LINEAR_ISSUE_KEY);
  const helloPath = path.join(workspacePath, 'tmp/hello.js');
  if (!existsSync(helloPath)) {
    return Result.fail({
      kind: 'VerifyFailed',
      check: 'fileExists',
      detail: `${helloPath} does not exist`,
      workspacePath,
    });
  }
  let stdout: string;
  try {
    const r = await execFileP('node', [helloPath]);
    stdout = r.stdout;
  } catch (err) {
    return Result.fail({
      kind: 'VerifyFailed',
      check: 'nodeOutput',
      detail: `node ${helloPath} failed: ${err instanceof Error ? err.message : String(err)}`,
      workspacePath,
    });
  }
  if (stdout !== 'Hello, World!\n') {
    return Result.fail({
      kind: 'VerifyFailed',
      check: 'nodeOutput',
      detail: `expected "Hello, World!\\n", got ${JSON.stringify(stdout)}`,
      workspacePath,
    });
  }
  try {
    const r = await execFileP('git', ['-C', workspacePath, 'log', '--oneline', '--', 'tmp/hello.js']);
    if (r.stdout.trim().length === 0) {
      return Result.fail({
        kind: 'VerifyFailed',
        check: 'gitCommitted',
        detail: 'no commits touch tmp/hello.js',
        workspacePath,
      });
    }
  } catch (err) {
    return Result.fail({
      kind: 'VerifyFailed',
      check: 'gitCommitted',
      detail: `git log failed: ${err instanceof Error ? err.message : String(err)}`,
      workspacePath,
    });
  }
  return Result.succeed(undefined);
}
```

- [ ] **Step 3: `main()` を完成させる (PASS まで)**

`main()` の `// TODO: verify` ブロックを以下に置換:

```ts
  console.log('→ verify workspace');
  const verifyResult = await verifyWorkspace(env);
  if (Result.isFailure(verifyResult)) return reportError(verifyResult.error);

  console.log('→ cleanup workspace (success)');
  await cleanWorkspace(env);

  console.log('PASS');
  return 0;
```

- [ ] **Step 4: フル E2E 実行**

```bash
cd /workspace
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```

Expected: 全ステップ通過、最後に `PASS` → exit 0。`/tmp/concert-e2e/workspaces/CYFY-5` は削除されている。

連続実行確認 (リセット動作):
```bash
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```
Expected: 1 回目と同じく PASS。

- [ ] **Step 5: VerifyFailed 経路の確認 (workspace を残すか)**

artificially fail させて確認:
```bash
# CYFY-5 の workflow.claude.md prompt を一時的に「hello.js を作らない」内容にして実行 → VerifyFailed が出るか
# (運用上は望ましくないので必要なら skip 可、後続タスクの acceptance check で代用)
```

時間がなければ Task 11 (受け入れ条件) で確認するため**ここはスキップ可**。

- [ ] **Step 6: commit**

```bash
cd /workspace
git add scripts/e2e.ts
git commit -m "$(cat <<'EOF'
feat(scripts): verify workspace and finalize e2e.ts pipeline

verifyWorkspace asserts tmp/hello.js exists, that node tmp/hello.js
prints exactly "Hello, World!\n", and that tmp/hello.js appears in git
history. main() now chains every step and prints PASS on success after
cleaning the workspace.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: 仕上げ — `.gitignore` の `scripts/e2e.symphony.log`、エラー時 workspace 保持確認

**Files:**
- Modify: `/workspace/.gitignore`

- [ ] **Step 1: `.gitignore` に Symphony ログを追加**

`scripts/dist/` の下に追記:

```diff
 # Build outputs
 scripts/dist/
+
+# E2E run artifacts
+scripts/e2e.symphony.log
```

- [ ] **Step 2: 既に存在するログを git に間違って add していないか確認**

Run: `git status` で `scripts/e2e.symphony.log` が untracked または ignored になっていることを確認。

- [ ] **Step 3: コード review — エラー時に workspace を残しているか**

`/workspace/scripts/e2e.ts` を読み返し、`VerifyFailed` 系で `cleanWorkspace` を呼ばないことを確認 (Result.isFailure で reportError して return する経路は cleanup を通らない、`main` の最後の cleanup は success path にしか到達しない)。

問題があれば修正。

- [ ] **Step 4: commit**

```bash
cd /workspace
git add .gitignore
git commit -m "$(cat <<'EOF'
chore(repo): ignore scripts/e2e.symphony.log

The e2e harness appends Symphony's TUI output to this file for
debugging. It rolls each run and shouldn't be tracked.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: ドキュメント整合スイープ

**Files:**
- Modify: `/workspace/CLAUDE.md`
- Modify: `/workspace/README.md`
- Modify: `/workspace/TODO.md`
- Modify: `/workspace/docs/e2e_testing.md`
- Modify: `/workspace/docs/adr/0001-adopt-monorepo.md`
- Modify: `/workspace/apps/claude-app-server/CLAUDE.md`
- Modify: `/workspace/apps/symphony/CLAUDE.md`

各ファイルで該当箇所のみ書き換え (他は触らない)。

- [ ] **Step 1: ルート `CLAUDE.md`**

検索: `scripts/e2e.sh` / `mise exec`

L228 周辺:
```diff
-疎通テストは `scripts/e2e.sh` が担当する（書く必要あり、内容は下記）。
+疎通テストは `pnpm e2e` (→ `scripts/e2e.ts`) が担当する。実装と詳細は `docs/superpowers/specs/2026-05-14-e2e-script-design.md` 参照。
```

L232〜258 の bash サンプル全部 (`#!/usr/bin/env bash` から始まる code block) を以下で置き換え:

````diff
-`scripts/e2e.sh` の中身は最終的にこんな形になる:
-
-```bash
-#!/usr/bin/env bash
-set -euo pipefail
-
-# 1. 両アプリビルド
-echo "→ Building claude-app-server..."
-(cd apps/claude-app-server && pnpm install && pnpm build)
-
-echo "→ Building symphony..."
-(cd apps/symphony && mix deps.get && mix compile)
-
-# 2. claude-app-server を PATH に通す
-export PATH="$(pwd)/apps/claude-app-server/dist:$PATH"
-chmod +x apps/claude-app-server/dist/bin.js
-
-# 3. Symphony を test workflow で起動
-echo "→ Running E2E with examples/test-workflow.md..."
-(cd apps/symphony && timeout 120 mise exec -- ./bin/symphony ../../examples/test-workflow.md)
-# → 期待: テスト用 issue 1個が処理されて完了
-```
-
-実 Linear を使うか、モックを使うかは別途決定。最初はモックで通すのが安全。
+`scripts/e2e.ts` (TypeScript、`pnpm e2e` で起動) が `pnpm build` → `pnpm dev:install` → Linear issue リセット → Symphony 起動 → workspace 検証を一発で実行する。Linear API key と test issue (default: `CYFY-5`) が必要。詳細は `docs/superpowers/specs/2026-05-14-e2e-script-design.md` と `docs/e2e_testing.md` を参照。
````

L373 周辺:
```diff
-- [ ] `scripts/e2e.sh` で受け入れテストが通る
+- [x] `pnpm e2e` (`scripts/e2e.ts`) で受け入れテストが通る
```

- [ ] **Step 2: ルート `README.md`**

L61〜75 周辺:
```diff
-./scripts/build_all.sh
+pnpm build
```
```diff
-./scripts/install_dev.sh
+pnpm dev:install
```
```diff
-mise exec -- ./bin/symphony examples/workflow.claude.md
+./bin/symphony examples/workflow.claude.md
```

L75 周辺:
```diff
-E2E テスト（`tmp/hello.js` を作って実行確認するシナリオ）は [`docs/e2e_testing.md`](docs/e2e_testing.md) を参照。`scripts/e2e.sh` で全部自動化される予定です。
+E2E テスト（`tmp/hello.js` を作って実行確認するシナリオ）は [`docs/e2e_testing.md`](docs/e2e_testing.md) を参照。`pnpm e2e` (→ `scripts/e2e.ts`) で自動化されています。
```

- [ ] **Step 3: `TODO.md`**

L22:
```diff
-6. モノレポ統合（`scripts/install_dev.sh`, `scripts/build_all.sh`）
+6. モノレポ統合 (`pnpm dev:install`, `pnpm build`)
```

L24:
```diff
-8. `scripts/e2e.sh` で受け入れテスト
+8. `pnpm e2e` (`scripts/e2e.ts`) で受け入れテスト
```

L130:
```diff
-- [ ] `scripts/e2e.sh`（受け入れテスト一発実行、ルート CLAUDE.md 参照、実行順 8 で着手）
+- [x] `scripts/e2e.ts`（受け入れテスト一発実行、`pnpm e2e` で起動）
```

L140:
```diff
-- [ ] `scripts/e2e.sh` で受け入れテストが通る
+- [x] `pnpm e2e` で受け入れテストが通る
```

L138 周辺 (Milestone 1 完了条件):
- `apps/claude-app-server` Phase 1〜4 完了 はチェック済みのはず、確認のみ

- [ ] **Step 4: `docs/e2e_testing.md`**

ファイル冒頭から書き換えるので、まず Read で現状を確認 → 全体的に bash 版を TS 版に置き換える方針で更新。要点だけ:

- L55〜67 の build 手順を `pnpm build` / `pnpm dev:install` に
- L79〜95 の install 手順を `pnpm dev:install` に
- L155〜196 の e2e.sh サンプル全体を、 `scripts/e2e.ts` の説明 + `pnpm e2e` 一発に圧縮
- L160 / L217 / L234 の `scripts/e2e.sh` 言及を `scripts/e2e.ts` / `pnpm e2e` に
- L203 の `mise exec -- ./bin/symphony` を `./bin/symphony --i-understand-...` に
- 新規セクション「5.4 `.env.example` と必要な環境変数」を追加 (env 変数一覧表、`cp .env.example .env` の手動運用、各変数のデフォルト)

仔細は executor の判断に任せるが、最終的に `mise exec` / `build_all.sh` / `install_dev.sh` / `scripts/e2e.sh` の文字列が**1つも残っていない**ことを以下で確認:

```bash
grep -nE "scripts/e2e\.sh|scripts/build_all\.sh|scripts/install_dev\.sh|mise exec" /workspace/docs/e2e_testing.md
# 何もヒットしないこと
```

- [ ] **Step 5: `docs/adr/0001-adopt-monorepo.md`**

L46:
```diff
-  - `scripts/e2e.sh` で両アプリのビルド〜疎通テストを 1 コマンドで実行できる
+  - `pnpm e2e` (`scripts/e2e.ts`) で両アプリのビルド〜疎通テストを 1 コマンドで実行できる
```

- [ ] **Step 6: `apps/claude-app-server/CLAUDE.md`**

L283:
```diff
-ルート `scripts/e2e.sh` 経由で。ユーザと協調。
+ルート `pnpm e2e` (`scripts/e2e.ts`) 経由で。ユーザと協調。
```

L306:
```diff
-- [ ] ルートの `scripts/e2e.sh` から呼び出される統合テストに対応
+- [x] ルートの `pnpm e2e` (`scripts/e2e.ts`) から呼び出される統合テストに対応
```

- [ ] **Step 7: `apps/symphony/CLAUDE.md`**

L185:
```diff
-mise exec -- mix test
+mix test
```

L193:
```diff
-mise exec -- ./bin/symphony examples/workflow.claude.md
+./bin/symphony --i-understand-that-this-will-be-running-without-the-usual-guardrails examples/workflow.claude.md
```

L197:
```diff
-ルートの `scripts/e2e.sh` から呼び出される統合テストにも対応すること。
+ルートの `pnpm e2e` (`scripts/e2e.ts`) から呼び出される統合テストにも対応すること。
```

L219:
```diff
-- [ ] ルートの `scripts/e2e.sh` から呼び出される統合テストに対応
+- [x] ルートの `pnpm e2e` (`scripts/e2e.ts`) から呼び出される統合テストに対応
```

- [ ] **Step 8: 最終確認 grep**

```bash
cd /workspace
grep -rnE "scripts/e2e\.sh|scripts/build_all\.sh|scripts/install_dev\.sh|mise exec" \
  --include='*.md' . \
  | grep -v node_modules | grep -v _build | grep -v docs/vendor \
  | grep -v docs/superpowers/specs/2026-05- \
  | grep -v docs/superpowers/plans/2026-05- \
  | grep -v apps/symphony/README.md \
  | grep -v apps/symphony/WORKFLOW.md
```

Expected: 出力ゼロ (本家由来と過去 spec/plan 以外のヒットがない)。

- [ ] **Step 9: commit**

```bash
cd /workspace
git add CLAUDE.md README.md TODO.md docs/e2e_testing.md docs/adr/0001-adopt-monorepo.md apps/claude-app-server/CLAUDE.md apps/symphony/CLAUDE.md
git commit -m "$(cat <<'EOF'
docs: replace scripts/e2e.sh / build_all.sh / install_dev.sh / mise exec with pnpm-based equivalents

Sweep all live docs (CLAUDE.md, README.md, TODO.md, docs/e2e_testing.md,
docs/adr/0001-adopt-monorepo.md, app-level CLAUDE.md) so they point at
scripts/e2e.ts and the pnpm scripts that actually exist. Leaves
upstream Symphony docs (apps/symphony/{README,WORKFLOW}.md) and
historical specs/plans untouched.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: 受け入れ条件の確認 (手動)

このタスクはコード変更を伴わない。受け入れ条件をユーザと一緒に通すか、executor が単独で実機実行する。

- [ ] **Step 1: 連続 2 回成功**

```bash
cd /workspace
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit1=$?"
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit2=$?"
```

Expected: 両方とも `PASS` + exit 0。

- [ ] **Step 2: ConfigError**

```bash
unset LINEAR_API_KEY
E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```
Expected: `FAIL: invalid env` + LINEAR_API_KEY issue + exit 2。

- [ ] **Step 3: LinearGraphQLError / Invalid (BOGUS key)**

```bash
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=BOGUS-9999 pnpm e2e
echo "exit=$?"
```
Expected: `LinearGraphQLError` か `LinearResponseInvalid` + exit 1。

- [ ] **Step 4: SymphonyTimedOut**

```bash
E2E_TIMEOUT_SECONDS=5 LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
echo "exit=$?"
```
Expected: `SymphonyTimedOut` + log path 表示 + exit 1。CYFY-5 は途中状態 (In Progress) になるはず → 再リセット必要。

- [ ] **Step 5: VerifyFailed (任意、Symphony 出力を作為的に壊すのが手間なら skip)**

CYFY-5 の description を一時的に `tmp/hello.js` ではなく `tmp/wrong.js` を作らせる内容に書き換えて pnpm e2e → `VerifyFailed: fileExists` を確認。確認後 description を元に戻す。

- [ ] **Step 6: workspace 残存ポリシー確認**

成功時:
```bash
ls /tmp/concert-e2e/workspaces/
```
Expected: 空 (CYFY-5 ディレクトリは削除されている)。

失敗時 (Step 4 の後):
```bash
ls /tmp/concert-e2e/workspaces/CYFY-5/
```
Expected: ディレクトリが残っている。

- [ ] **Step 7: SIGINT (Ctrl+C) 動作**

別ターミナルから:
```bash
LINEAR_API_KEY="$LINEAR_API_KEY" E2E_LINEAR_ISSUE_KEY=CYFY-5 pnpm e2e
# spawn symphony が始まったあたりで Ctrl+C
echo "exit=$?"
```
Expected: `FAIL: interrupted` + exit 130。Symphony 子プロセスが残っていないこと:
```bash
pgrep -af nil_escript || echo "ok"
```

- [ ] **Step 8: 全工程通ったら TODO.md と CLAUDE.md の Milestone 1 完了条件を再確認**

Run: `grep -n "Phase 1〜2 完了\|claude-app-server. Phase\|scripts/e2e.sh\|pnpm e2e" /workspace/TODO.md` で、checkbox がすべて埋まっていることを確認。

- [ ] **Step 9: 受け入れ条件パス報告**

executor は受け入れ条件のうち通った項目をユーザに報告し、`docs/superpowers/plans/2026-05-14-e2e-script.md` の本 Task 11 チェックを埋める (任意)。

---

## Self-Review チェック (executor 用)

実装完了後、以下を確認:

1. **`grep -E "scripts/e2e\.sh|scripts/build_all\.sh|scripts/install_dev\.sh|mise exec"` がライブドキュメントでゼロヒット** (本家由来と過去 spec/plan は除外)
2. **`pnpm e2e` の連続 2 回成功**
3. **`E2EError` のすべての kind が `reportError` の switch でカバーされている** (`assertNever` の到達がコンパイル時に検出される)
4. **`scripts/dist/` と `scripts/e2e.symphony.log` が `.gitignore` 済み**
5. **`apps/symphony/README.md` と `apps/symphony/WORKFLOW.md` は触っていない** (本家由来は除外する方針)
